# OCTera

Интеллектуальная медицинская AI-платформа для автоматического анализа оптической когерентной томографии (ОКТ) сетчатки глаза.

Платформа предназначена для помощи врачам-офтальмологам в диагностике заболеваний глаз путем анализа медицинских изображений с использованием современных технологий компьютерного зрения и глубокого обучения.

OCTera не заменяет врача, а выступает в роли интеллектуального ассистента, который за несколько секунд анализирует ОКТ-снимок, обнаруживает патологические изменения, оценивает вероятность различных заболеваний, выделяет проблемные области и формирует предварительное медицинское заключение.

## Текущий этап продукта

Реализованный MVP — **self-service сервис** по модели ChatGPT/Claude: любой человек сам регистрируется и загружает собственные ОКТ-снимки, без понятия "врач ведёт карточки пациентов". Монетизация в этой модели — будущие подписки (см. «Бизнес-модель» ниже).

Клиническая/B2B-модель (несколько пациентов на одного врача, история и динамика по пациенту, PACS-интеграции, роли в клинике) — описана в разделах ниже как долгосрочное направление, но пока не реализована в продукте; технически для неё уже есть задел (в БД учётная запись пользователя 1-в-1 связана с профилем пациента, откуда при необходимости можно вырастить полноценную многопользовательскую модель).

## Разработка

Репозиторий — монорепо из двух частей:

- `backend/` — FastAPI-сервис: REST API, аутентификация, хранение исследований, оркестрация AI-пайплайна (`app/services/pipeline.py`).
  - **Quality-check** (`app/services/quality_check.py`) — реальная статистика по загруженному изображению: разрешение, резкость (дисперсия Лапласиана), экспозиция.
  - **Сегментация** (`app/services/segmentation.py`) — реальный сигнал-процессинг по пикселям снимка: границы слоёв отслеживаются **по каждому столбцу скана отдельно** (одно кандидатное положение на A-scan, привязка к ближайшему пику у соседнего столбца для непрерывности), а не одной усреднённой по всей ширине линией — на реальном изогнутом (не идеально плоском) снимке сетчатки, где глубина плывёт от столбца к столбцу, единая усреднённая по ширине линия давала бессмысленную, «размазанную» границу. Итоговая картинка — это сам снимок с закрашенными полупрозрачным цветом слоями и подписанной легендой (а не отдельная карта градиентов, которая ничего не подписывала и не показывала). Толщина слоёв в отчёте усредняется по всем столбцам. Не обучаемая модель и не per-pixel сегментация — честная эвристика на реальных данных снимка, а не случайные числа. Пересчёт в микроны использует условный коэффициент (нет калибровки скана) — абсолютные значения толщины иллюстративные. Точка, куда подключается обученная U-Net/nnU-Net при появлении размеченного датасета.
  - **Карта патологий** (та же `segmentation.py`) — алгоритмически находит гипорефлективные (тёмные) локализованные зоны внутри слоёв сетчатки — классический прокси для жидкости/кист/отслойки на ОКТ (они оптически "пустые", т.е. тёмные). Область поиска (верх/низ сетчатки на каждом столбце) определяется отдельным, более грубым и устойчивым детектором «где вообще есть ткань» (`_tissue_extent`), не тем же самым мелким послойным трекингом, что рисует карту слоёв выше — крупная патология (например, большая кистозная полость) может сбить с толку потонкий послойный трекинг именно в области находки, но грубая оценка «где ткань» остаётся надёжной и там. Это простой heuristic (локальный порог по яркости + фильтр по форме пятна), **не диагноз и не классификатор типа находки** — для точного разделения "киста vs жидкость vs артефакт" нужна модель, обученная на попиксельной разметке, которой пока нет. В интерфейсе и в тексте заключения явно помечено как "требует проверки врачом".
  - **Диагностика** (`app/services/diagnosis.py`) — реально обученный классификатор из `app/ml/` (см. ниже).
  - **Генератор заключения** (`app/services/report_generator.py`) — шаблонная сборка текста из структурированных находок, включая предупреждение при низкой уверенности модели (`LOW_CONFIDENCE_THRESHOLD`); описан как точка для подключения LLM (например, через Ollama — см. обсуждение ниже), но сама LLM пока не подключена (её веса тоже нужно откуда-то скачать, а публичные реестры моделей недоступны из этой песочницы).
- `frontend/` — личный кабинет на Next.js (App Router, TypeScript): вход/регистрация, прямая загрузка ОКТ-снимка, список своих исследований, страница результата (снимок, карта, толщина слоёв, вероятности диагнозов, текст заключения). Каждый пользователь видит только свои исследования (проверка владения на бэкенде, см. `app/api/deps.py:get_current_patient`).

### ML-классификатор диагнозов (`backend/app/ml/`)

Модель обучена на **реальных ОКТ-снимках пациентов**, а не на синтетике. Датасет: [OCT-AND-EYE-FUNDUS-DATASET](https://github.com/Traslational-Visual-Health-Laboratory/OCT-AND-EYE-FUNDUS-DATASET) (Hughes Cano, Olivares Pinto & Thébault; CONACYT/UNAM/IMO/APEC/INDEREB) — 1113 ОКТ-снимков с диагнозом диабетического макулярного отёка (DME), установленным врачами-офтальмологами. Классификация бинарная: `NORMAL` / `DME` — датасет не размечает другие патологии (CNV, друзы и т.п.), поэтому модель пока предсказывает только их.

`app/ml/train.py` подбирает лучший классификатор кросс-валидацией (сбалансированная точность, т.к. DME — миноритарный класс: 167 из 1113) среди нескольких кандидатов (MLP разных размеров, RandomForest, SVM с `class_weight="balanced"`, SVM после PCA(30) — см. round 11 — каждый обёрнут в `StandardScaler`). Признаки (`app/ml/features.py`): **HOG** (гистограммы ориентированных градиентов, 64×64) — они улавливают слоистую текстуру ОКТ гораздо лучше сырых пикселей — плюс доменные признаки, производные от того же сигнал-анализа, что и сегментация (протяжённость/толщина сетчаточной полосы, доля гипорефлективных зон = жидкость). Переход с сырых пикселей на HOG измеренно снизил долю ложных тревог по DME почти вдвое (см. историю коммитов).

**Текущие метрики** (см. полностью в `app/ml/artifacts/metrics.json`; пересчитывались несколько раз — round 10 исправил утечку по пациентам, round 11 сменил модель, round 14 нашёл ещё одну утечку, round 15 добавил усреднение по сидам, round 23 добавил аугментацию поворотом, round 27 исправил число компонент PCA и сам процесс отбора модели, см. ниже): на конкретном воспроизводимом чекпоинте (`random_state=42`, тот самый, что реально сохранён и используется в проде — `svm_rbf_pca75`, отобран автоматически через CV после исправления round 27) — test accuracy 93.6%, для класса DME precision 0.77 / recall 0.90 (дефолтный 50%-порог) либо precision 0.72 / recall **0.93** на CV-подобранном чувствительном скрининговом пороге (`DME_SCREENING_THRESHOLD` = 0.123, читается из `metrics.json` автоматически, а не хардкодится). Поле `multi_seed_evaluation` в `metrics.json` (test accuracy 94.2% ±1.3 п.п., DME precision 0.79 ±0.08 / recall 0.79 ±0.09 на дефолтном пороге) честно переобучает именно этот рецепт (аугментация поворотом + PCA(75), отбор модели на аугментированных данных) на каждом из 5 сидов. Это реальная, но не клинически валидированная модель — она демонстрирует механизм, а не готова к диагностическому использованию.

**ML round 4** (попытка поднять DME recall): `MLPClassifier` (лучшая модель по CV) не поддерживает `class_weight`, поэтому испытан oversampling миноритарного класса аугментированными копиями (`app/ml/train.py`, флаг `--minority-oversample`, по умолчанию выключен). Результат отрицательный: при факторах 2x и 4x модель на held-out тесте пропускала **ровно те же 11 снимков** DME (recall остался 0.67), а precision просел до 0.88/0.81 — то есть дисбаланс классов не был узким местом, узкое место — потолок самих признаков (HOG + доменные) на этих конкретных снимках. Флаг оставлен в коде для дальнейших экспериментов, но по умолчанию выключен, чтобы не проиграть уже имеющемуся балансу precision/recall.

**ML round 5** (анализ порога классификации, ⚠️ метод исправлен в round 6 — см. ниже): в отличие от oversampling, порог реально двигает баланс. `train.py` считал precision-recall кривую по вероятности DME **на held-out тесте** и писал в `metrics.json` две альтернативные рабочие точки:
  - лучший F1 при cutoff≈0.53 (почти тот же, что дефолтный 0.5): precision 0.96 / recall 0.67 — то же количество пропущенных случаев;
  - **recall≥0.85** при cutoff≈0.018: precision 0.62 / recall 0.91 — модель пропускает только 3 из 33 DME-снимков вместо 11, ценой роста ложных срабатываний на здоровых снимках (18 из 190 вместо 2).

  По продуктовому решению (проверено с пользователем) этот порог применён в `app/services/report_generator.py` (`DME_SCREENING_THRESHOLD`): для скринингового ассистента, где каждую находку в любом случае проверяет врач, пропущенный отёк хуже лишней проверки. ДМО помечается как находка, требующая очной консультации, начиная с этой вероятности — а не только когда она выше 50% и формально «наиболее вероятна» — с явной пометкой о пониженной уверенности модели в пограничных случаях; сырые проценты по обоим классам в интерфейсе не меняются.

**ML round 6** (исправление утечки данных при подборе порога): подбор порога в round 5 был методологической ошибкой — precision-recall кривая считалась прямо на test-выборке, той же самой, на которой потом репортились финальные цифры. Это классическая утечка: порог, который лучше всего выглядит на конкретных 223 тестовых снимках, не обязательно обобщается на новые данные. Исправлено в `train.py` (`_cv_threshold_analysis`): пороги теперь подбираются через out-of-fold предсказания 4-fold CV **только на train-выборке** (890 снимков), а test-выборка используется один раз — просто чтобы честно оценить (не подобрать) уже выбранные пороги. Честные цифры оказались скромнее:
  - лучший F1 при cutoff≈0.135: precision 0.82 / recall 0.70 (было заявлено 0.96/0.67 — cutoff сместился, но recall почти не вырос);
  - **recall≥0.85 на train CV** при cutoff≈0.042: на честном held-out тесте recall 0.79 / precision 0.68 — заметно скромнее, чем заявленные ранее 0.91/0.62 (0.85 на CV не гарантирует 0.85 на новых данных).

  `DME_SCREENING_THRESHOLD` обновлён с 0.018 на 0.042 (честный recall≥0.85-на-CV порог) — сама продуктовая логика не менялась и уже была согласована в round 5, поменялось только конкретное число после исправления методологии.

**ML round 7** (тесты на логику подбора порога): функции `_select_thresholds`, `_metrics_at_threshold`, `_oversample_minority` были покрыты только сквозным прогоном `train.py` на реальном датасете — ни одного юнит-теста, при том что именно в этой логике уже проехала утечка данных (round 6). Добавлены юнит-тесты (`tests/test_threshold_selection.py`) на синтетических данных без зависимости от датасета. Попутно выяснилось: минимальный порог из `precision_recall_curve` почти всегда даёт recall=1.0 (при нём всё классифицируется как «позитив»), поэтому ветка `high_recall_threshold=None` практически недостижима для разумных `min_recall` — это не баг, но задокументировано тестом, чтобы никто не «исправил» эту ветку во что-то нерабочее.

**ML round 8** (калибровка вероятностей): проверено, не улучшит ли `CalibratedClassifierCV` (sigmoid/isotonic) качество вероятностей модели поверх работы над порогом (round 5-6). На честном held-out тесте:
  - без калибровки: Brier 0.053 / log loss 0.198;
  - **isotonic**: Brier 0.046 / log loss 0.144 — лучше по обеим метрикам в среднем по всей выборке;
  - **sigmoid**: Brier 0.053 / log loss 0.202 — практически без изменений.

  Но на конкретной рабочей точке (recall≥0.85-на-CV, уже применённой в проде) isotonic её портит: precision 0.58 / recall 0.76 против 0.68/0.79 без калибровки — то есть более точная калибровка «в среднем по вероятности» не гарантирует лучшую работу в конкретной точке отсечения, которую реально использует приложение. Sigmoid — нейтрален (не лучше и не хуже в пределах шума на 33 тестовых DME-снимках). Калибровка не применена в проде; `metrics.json` теперь дополнительно хранит `dme_calibration_metrics` (Brier/log loss без калибровки) для отслеживания при будущих переобучениях.

  ⚠️ Черновой скрипт для этого раунда изначально содержал баг в порядке столбцов при вызове `log_loss` (передавал 2D-массив вероятностей в порядке `classes_` вместо порядка меток `y_true`), из-за чего isotonic сперва выглядела катастрофически (log loss ~19 вместо честных 0.14). Исправлено до того, как ушло в `train.py` и в этот README — на будущее: `log_loss` для бинарного случая безопаснее вызывать с 1D-массивом вероятностей положительного класса, а не с 2D-массивом, порядок столбцов которого нужно сверять вручную.

**ML round 9** (текстурные признаки LBP): проверено, добавляют ли Local Binary Patterns (`skimage.feature.local_binary_pattern`) сигнал поверх HOG + доменных признаков — LBP часто хорошо ловит текстуру именно там, где HOG слабее (мелкая, немасштабная зернистость). Два варианта, оба на реальном датасете, тот же train/test-сплит, что и раньше:
  - простой LBP (P=8, R=1, 10-бинная гистограмма) отдельно: cv balanced accuracy 0.673 — намного хуже HOG+domain (0.807), ожидаемо для 10-мерного признака против 1578-мерного;
  - HOG+domain **+** простой LBP: cv 0.802 (не лучше 0.807), test precision 0.88 / recall 0.70 (было 0.92/0.67) — recall чуть вырос, precision чуть просел;
  - HOG+domain **+** multi-radius LBP (P/R = 8/1, 16/2, 24/3, 54 бина): cv 0.807 (то же самое, но с меньшим разбросом по фолдам: ±0.018 против ±0.039) и test precision 0.95 / recall 0.64 (наоборот, precision чуть вырос, recall чуть просел).

  Ни один вариант не даёт чистого улучшения — сдвиги precision/recall в разные стороны у двух конфигураций и в пределах шума на 33 тестовых DME-снимках (один снимок = ~3 п.п.). LBP не добавлен в `features.py`/прод; выводы задокументированы, чтобы не повторять этот же эксперимент.

**ML round 10** (утечка данных по пациентам — крупнее, чем round 6): имена файлов в датасете (`1222_OD_o_2`, `1222_OI_o_1`, ...) — это `<ID пациента>_<глаз>_o_<визит>`. Проверка показала: 1113 снимков принадлежат всего 831 уникальному пациенту — у 25% снимков есть «родственник» от того же пациента (оба глаза и/или повторные визиты). Использовавшийся до этого раунда сплит (`train_test_split` со стратификацией по классу, но **не по пациенту**) пускал снимки одного пациента одновременно и в train, и в test: **79 из 831 пациентов** пересекались между выборками. Модель могла частично опираться на специфичные для пациента признаки (уникальный рисунок сосудов, особенности конкретного визита/камеры), а не только на признаки самого ДМО — это завышало все метрики, включая заглавные "текущие метрики" выше.

  Исправлено: `_group_aware_split` (`train.py`) — `StratifiedGroupKFold` вместо `train_test_split`, группировка по ID пациента, гарантирующая, что ни один пациент не попадёт одновременно в обе выборки. То же самое исправление внесено во внутренние CV на train-выборке (`select_best_model`, `_cv_threshold_analysis`) — иначе группировка на верхнем уровне не спасла бы от такой же утечки внутри. Честные цифры на patient-grouped сплите:
  - held-out test accuracy 92.8% (было 94.2%), DME precision 0.84 / recall 0.64 (было 0.92 / 0.67);
  - **recall≥0.85-на-CV** порог сместился на 0.037 (было 0.042): честный test recall 0.88 / precision 0.57 (round 6 давал recall 0.79 / precision 0.68 на сплите с утечкой).

  `DME_SCREENING_THRESHOLD` обновлён на 0.037. Это снова техническая поправка методологии (сплит теперь корректен), а не новое продуктовое решение — сам выбор «флагировать ДМО ради recall» остаётся тем же, что согласован в round 5. Добавлен юнит-тест (`tests/test_group_split.py`), проверяющий, что ни один пациент не оказывается одновременно в train и test.

**ML round 11** (снижение размерности перед SVM): 1578-мерный HOG+domain вектор против ~700-900 обучающих снимков (после патч-группировки round 10) — классический high-dim/low-N режим, рискованный особенно для SVM. Проверен `PCA` разной размерности перед каждым кандидатом на честном patient-grouped CV:
  - `PCA(20-75)+MLP`: cv balanced accuracy 0.81-0.83 — в пределах шума относительно baseline MLP (0.817), не улучшение;
  - `PCA(30)+SVM`: cv balanced accuracy **0.879** (±0.013) — заметно выше любого варианта без понижения размерности (0.816-0.831), разница больше суммарного разброса по фолдам.

  `svm_rbf_pca30` добавлен как новый кандидат в `_candidate_estimators()` и честно выиграл отбор через существующий механизм `select_best_model` (не выбран вручную). На held-out тесте: accuracy 92.8% (то же самое), но DME precision 0.71 / recall 0.88 (F1 0.78) — заметно другой баланс, чем у прежней MLP (0.84/0.64, F1 0.72): новая модель уже на дефолтном 50%-cutoff даёт recall, сравнимый с тем, что раньше давал специальный скрининговый порог. Пересчитан и `DME_SCREENING_THRESHOLD` под новую модель — honest recall≥0.85-на-CV порог для неё 0.260 (test recall 0.88 / precision 0.66, примерно та же точка, что и дефолтный cutoff — подтверждена тем же методом CV-подбора для консистентности с round 6/10).

  Смена алгоритма (не только гиперпараметров) — это техническое решение через уже существующий, объективный механизм отбора модели по CV, а не новое клиническое решение; сам подход «показывать оба процента, флагировать по порогу» не менялся.

**ML round 12** (проверка устойчивости round 11): на датасете такого размера (33-51 DME в тесте, ~130-190 DME в train в зависимости от сплита) единичный CV-результат может оказаться просто удачным сидом — round 6 и round 10 уже показали, как легко здесь ошибиться. Перепроверено на нескольких случайных сидах, без изменений кода:
  - варьировался сид **фолдов CV** (тот же train/test, что в round 11): `PCA(30)+SVM` стабильно 0.857-0.883 против MLP 0.799-0.819 — отрыв сохраняется на всех сидах;
  - варьировался сид **самого train/test сплита**: `PCA(30)+SVM` снова стабильно впереди (cv 0.866-0.879 против MLP 0.761-0.817), test recall держится в диапазоне 0.86-0.94 (precision 0.71-0.80) на трёх разных сплитах.

  Round 11 подтверждён — это не артефакт конкретного сида `random_state=42`. Изменений в коде нет, это верификационный раунд.

**ML round 13** (тонкая настройка PCA(30)+SVM — не осталось ли лёгкого выигрыша): проверено, не близко ли выбранное в round 11 число компонент/гиперпараметры SVM к границе, где можно было бы легко улучшить результат.
  - число компонент PCA в районе 30: 15→0.867, 20→**0.881**, 25→0.875, 30→0.879, 35→0.878 — плато в пределах шума (±0.013-0.018), 30 не хуже соседних значений;
  - grid по `C`/`gamma` вокруг дефолтных `SVC(C=1.0, gamma="scale")`: ни одна из проверенных комбинаций (C∈{0.1,1,10}, gamma∈{scale,0.01,0.001}) не обошла дефолт (0.879); `gamma=0.01` вообще катастрофична (cv=0.5, случайное угадывание) — судя по всему, неподходящий масштаб для этих признаков после PCA.

  Уже выбранная конфигурация (round 11) оказалась близка к оптимуму в проверенной области — легко доступного выигрыша тонкой настройкой не нашлось. Изменений в коде нет.

**ML round 14** (утечка через дубликаты файлов — group-aware сплит round 10 не полная защита): по MD5-хешу содержимого файла найдено **12 пар байт-в-байт идентичных снимков**, оформленных в датасете под **разными** ID пациентов (соседние/близкие номера, например `1330` и `1348`) — похоже на артефакт дублирования записи в исходном датасете, не ошибку разметки: метки DME внутри каждой пары совпадают. Поскольку `_group_aware_split` группирует только по ID пациента, эти дубликаты его не защищают: на текущем сплите (`random_state=42`) **2 из 12 пар всё равно оказались по разные стороны** train/test — модель могла в буквальном смысле увидеть тот же снимок на обучении и тривиально узнать его на тесте.

  Исправлено: `_merge_duplicate_groups` (`train.py`) — union-find, объединяющий ID пациентов, если у них хоть один байт-идентичный снимок, до вызова `_group_aware_split`. Уникальных групп стало 820 (было 831). Добавлен юнит-тест (`tests/test_duplicate_groups.py`).

  Побочный эффект: объединение части групп в более крупные блоки грубее квантует стратификацию по классу — на конкретном `random_state=42` тестовая выборка сократилась с 33 до 25 DME-снимков, а precision на этом конкретном сплите просела до 0.47 (recall остался 0.84). Проверка на других сидах сплита (7, 99, 123) показала precision 0.61-0.72 при recall 0.82-0.90 — то есть 0.47 не признак того, что модель стала хуже, а особенность именно этого воспроизводимого разбиения. `DME_SCREENING_THRESHOLD` обновлён под честный recall≥0.85-на-CV порог для этого сплита — 0.350 (test recall 0.84 / precision 0.50).

**ML round 15** (усреднение по сидам сплита — вместо доверия одному числу): round 14 показал, что precision на этом маленьком, сгруппированном по пациентам датасете может скакать от 0.47 до 0.72 в зависимости от сида сплита — единичное число легко может ввести в заблуждение. `train.py` теперь честно переобучает **тот же тип модели**, что победил в отборе, на 5 разных сплитах (сиды 42, 7, 99, 123, 2024) и пишет среднее±стд в `metrics.json` (`multi_seed_evaluation`), используя только held-out тестовые метрики (без изменения того, на чём в тот момент обучался финальный чекпоинт — см. round 16 про то, что изменилось дальше).

  Результат: test accuracy 0.905±0.016, DME precision 0.643±0.096, DME recall 0.865±0.038 — сплит `random_state=42` (precision 0.47) оказался на пессимистичном краю именно по precision, но recall и accuracy у него вполне типичные. Заглавные метрики выше теперь приводят оба числа — усреднённое (типичное) и то, что было на конкретном сплите — вместо того, чтобы выдавать один сплит за окончательную правду.

**ML round 16** (финальный чекпоинт — на 100% данных, а не только на 80%): все метрики выше честно измерены на модели, обученной только на train-сплите (~80% данных) — это правильный способ оценить обобщение. Но датасет маленький (1113 снимков, из них только 167 DME), и каждый размеченный снимок ценен. Стандартная практика ML: после того как валидация подтвердила качество подхода, финальный чекпоинт **дообучается на всех 100% данных** перед отправкой в прод — это не может ухудшить результат (может только помочь, при условии отсутствия сдвига распределения между train и test, что здесь так и есть, поскольку тестовые снимки взяты из того же датасета).

  Реализовано в `train.py`: после подсчёта held-out метрик, анализа порога, калибровки и multi-seed оценки — те по-прежнему честно измерены на модели, обученной только на train-сплите — финальный чекпоинт (`model = OCTClassifier(...); model.fit(X_all, y_all)`) переобучается на всех 1113 снимках того же типа модели (`svm_rbf_pca30`), что выиграл отбор, и именно этот чекпоинт сохраняется и используется в проде. `metrics.json` получил поле `shipped_checkpoint_trained_on_full_dataset: true`, явно поясняющее, что цифры в model card — честная оценка *подхода*, а не прямое измерение производительности именно этого финального артефакта (который не был напрямую протестирован на held-out данных, поскольку все они уже пошли в его обучение).

**ML round 17** (ансамбль и RandomForest+PCA — оба хуже одиночного SVM): проверено, не улучшит ли `svm_rbf_pca30` (round 11) комбинирование с другими моделями на честном patient-grouped CV:
  - `RandomForest` после `PCA(30)`: cv balanced accuracy 0.624 — намного хуже одиночного SVM (0.902), PCA явно не помогает RandomForest так же, как SVM;
  - `VotingClassifier` (soft voting, `svm_rbf_pca30` + `mlp_64`), равные веса: cv 0.855 — хуже одиночного SVM;
  - тот же voting с весами 2:1 в пользу SVM: cv 0.858 — всё ещё хуже.

  И ансамблирование, и RF+PCA тянут результат вниз, а не вверх: `svm_rbf_pca30` настолько сильнее слабых моделей, что усреднение вероятностей с ними просто портит его собственный, уже хороший результат. Изменений в коде нет.

**ML round 18** (доменные признаки всё ещё нужны? — перепроверка на новом пайплайне): в round 3 доменные признаки (толщина сетчаточной полосы, доля гипорефлективных зон — всего 10 чисел) добавили к HOG и получили «небольшой, но измеримый прирост» — но это было ещё на старой MLP-модели, до патч-группировки (round 10) и смены модели на PCA+SVM (round 11). Перепроверено на честном patient-grouped CV с текущим пайплайном:
  - `HOG + domain` (текущий, `svm_rbf_pca30`): cv 0.902;
  - только `HOG` (те же PCA(30)+SVM): cv 0.894 — разница в пределах шума фолдов (±0.024-0.027);
  - только доменные признаки (10 чисел, без PCA — размерность и так низкая): cv **0.859** — удивительно сильный результат для всего 10 простых чисел, почти на уровне полного HOG-вектора (1568-мерного).

  Комбинация по-прежнему оправдана (0.902 — лучшее число из трёх), хоть прирост от добавления HOG к домену (или домена к HOG) и небольшой относительно шума. Главный интересный вывод — доменные признаки несут реальный, существенный сигнал сами по себе, а не просто шум: это хорошая независимая проверка, что эвристика сегментации (`segmentation.py`) действительно улавливает что-то осмысленное про толщину сетчатки и тёмные зоны, а не выдаёт случайные числа. Изменений в коде нет.

**ML round 19** (⚠️ устойчивость к реальным фото — найдена серьёзная уязвимость к повороту): толчком послужил реальный снимок пользователя — фотография экрана/распечатки ОКТ с телефона, а не прямой цифровой экспорт, как в датасете обучения. Проверена устойчивость шипованной модели (`svm_rbf_pca30`) к типичным искажениям такой съёмки — на held-out тестовой выборке, к каждому снимку применено одно из искажений:
  - **JPEG-сжатие** (quality=30 и даже quality=10 — сильное сжатие): accuracy 0.947-0.956, DME precision/recall не хуже базовых (0.68/0.92) — модель устойчива;
  - **яркость** (×0.7 темнее / ×1.4 ярче, имитация недосвета/пересвета) и **контраст** (×0.6, «смытое» фото): accuracy 0.947-0.951 — тоже устойчива;
  - **поворот на 5°**: accuracy падает до 0.893-0.911, DME precision — с 0.68 до 0.51-0.56;
  - **поворот на 15°**: accuracy 0.591, DME precision 0.05 / recall 0.16 — модель полностью ломается, предсказания на уровне случайного шума.

  Причина ожидаема и объяснима: доменные признаки (`segmentation.py`) строятся на построчном анализе яркости вдоль оси глубины скана — поворот снимка полностью искажает этот профиль; HOG тоже завязан на фиксированную сетку ячеек, чувствительную к ориентации. Для self-service платформы, где пользователи сами фотографируют распечатки/экраны (ровно как в присланном примере), даже небольшой перекос камеры — совершенно реалистичный сценарий, и сейчас он способен незаметно сломать анализ без явного предупреждения пользователю. Это не пофикшено в этом раунде (требует либо детектора перекоса в `quality_check.py`, либо признаков, устойчивых к повороту, — оба варианта нетривиальны и заслуживают отдельного раунда/продуктового решения), но задокументировано как реальный, воспроизводимый пробел, а не гипотетический.

**ML round 20** (попытка авто-deskew — неоднозначный результат, не внедрён): проверен самый очевидный фикс round 19 — перебор угла поворота (от -20° до +20°, шаг 2°) с выбором угла, максимизирующего «пиковость» (стд.) построчного профиля яркости, перед извлечением признаков:
  - **поворот 15° + deskew**: accuracy восстанавливается с 0.591 до **0.898** (средний найденный угол коррекции 14.0°, близко к истинным 15°) — эвристика реально работает на сильных поворотах;
  - **поворот 5° + deskew**: 0.898 vs 0.893 без коррекции — почти не меняется (при малом повороте пиковость не даёт chёткого сигнала);
  - **⚠️ baseline БЕЗ поворота + deskew** (проверка на «не навреди»): accuracy **падает** с 0.942 до 0.880 — эвристика иногда находит ложный «оптимальный» угол (в среднем 4.3°) даже на уже правильно ориентированных снимках, портя то, что не было сломано.

  Deskew — реальный, но обоюдоострый инструмент: спасает сильно повёрнутые снимки, но вредит нормальным. Раз в реальном датасете обучения снимки уже выровнены (round 19 не находил проблем на них), включать коррекцию для всех подряд — чистый проигрыш на типичном случае. Не внедрено в прод: нужен более умный триггер (например, применять коррекцию только когда есть независимый сигнал, что снимок действительно повёрнут, а не всегда) — это отдельная, нетривиальная задача, оставлена как открытый пункт вместе с round 19.

**ML round 21** (попытка триггера для deskew — тоже не сработала): проверено самое очевидное решение открытого вопроса round 20 — применять коррекцию только если найденный угол даёт заметный выигрыш «пиковости» относительно угла 0° (порог `margin`), а не всегда брать лучший угол. Перебраны 4 значения порога (1.05 / 1.08 / 1.15 / 1.25 — требуемое относительное превышение пиковости):

  | margin | baseline (без поворота) | доля «исправленных» | поворот 5° | поворот 15° |
  |---|---|---|---|---|
  | без триггера (round 20) | 0.880 | 100% | 0.898 | 0.898 |
  | 1.05 | 0.880 | 48% | 0.907 | 0.898 |
  | 1.08 | 0.880 | 42% | 0.902 | 0.898 |
  | 1.15 | 0.889 | 30% | 0.893 | 0.889 |
  | 1.25 | 0.898 | 18% | **0.889** (хуже, чем без коррекции: 0.893) | 0.867 |
  | без поворота, без deskew (истинный baseline) | **0.942** | — | — | — |

  Чистого порога не нашлось: с ростом margin доля ложных «исправлений» на нормальных снимках падает (48%→18%), но baseline accuracy так и не приближается к истинным 0.942 (максимум 0.898), а чувствительность к реальным поворотам падает пропорционально — при margin=1.25 коррекция для умеренного поворота (5°) уже хуже, чем вообще её не делать (0.889 vs 0.893). Причина: «пиковость» построчного профиля яркости и так сильно варьируется между снимками из-за анатомии и качества съёмки — эта естественная вариация того же порядка, что и сигнал от самого поворота, поэтому простой относительный порог не может надёжно отличить одно от другого. Нужен принципиально другой, независимый сигнал перекоса (например, поиск доминирующих линий/краёв через преобразование Хафа), а не повторное использование той же метрики, что и для решения о коррекции. Изменений в коде нет — открытый вопрос round 19/20 остаётся открытым.

**ML round 22** (независимый сигнал перекоса — тоже слишком шумный, и стало ясно почему): реализован сигнал, который round 21 предложил как альтернативу «пиковости» — доминирующая ориентация градиента (через приём с удвоением угла, устойчивый к 180°-неоднозначности направления края), которая для правильно ориентированного скана должна быть вертикальной; отклонение от вертикали = оценка угла поворота. Проверено на 60 тестовых снимках с разными истинными углами поворота (0°/5°/15°), с вариантами по порогу силы градиента (95-й/98-й перцентиль) и по области анализа (весь снимок vs только зона сетчатки):

  - оценка **несмещённая в среднем**: медиана при истинном 0° близка к 0, при 5° — около -5, при 15° — около -15 (знак — просто соглашение направления);
  - но **очень шумная на уровне отдельного снимка**: std 12-17° почти во всех вариантах — сопоставимо с самим сигналом от поворота на 5-15°;
  - лучший найденный вариант (98-й перцентиль, только зона сетчатки) снизил std для угла 5° до **6.0°** — заметное улучшение, но std на истинном 0° почти не изменился (11.8°), то есть по-прежнему множество лже-срабатываний на нормальных снимках.

  Более глубокий вывод, чем просто «метрика неудачная»: скорее всего дело в том, что у разных пациентов заметно отличается **естественная кривизна сетчатки** (у кого-то более плоская, у кого-то более выпуклая) — это варьирует «глобальную ориентацию» снимка ничуть не меньше, чем реальный поворот камеры на 5-15°. Простая глобальная оценка ориентации (любым методом — через яркость или через градиент) не может отличить «камеру наклонили» от «у этого пациента такая форма сетчатки». Надёжное решение потребовало бы моделировать саму форму кривой границы сетчатки и искать асимметрию относительно неё, а не глобальный угол — это существенно больший объём работы, выходящий за рамки одного раунда. Изменений в коде нет; открытый вопрос round 19-21 остаётся открытым, но диагноз стал точнее.

**ML round 23** (⚠️ устойчивость к повороту через аугментацию обучения — реальный выигрыш, но с честной оговоркой о recall): rounds 20-22 пытались обнаружить/исправить поворот во время инференса и не нашли надёжного сигнала (см. выше). Round 23 пробует другой подход — заложить устойчивость прямо в обучение: к каждому обучающему снимку добавляются копии, повёрнутые на -10°/-5°/+5°/+10° (`_rotation_augment` в `train.py`), с сохранением группы пациента у каждой копии (та же защита от утечки, что и в `_oversample_minority` выше — иначе повёрнутые копии одного снимка могли бы разъехаться по разным фолдам group-aware CV). На честном held-out тесте (тот же сплит, что и в round 19):

  | | без поворота | +5° | +15° | +20° (за пределами диапазона аугментации) |
  |---|---|---|---|---|
  | базовая модель (без аугментации) | acc 0.876, DME p=0.47/r=0.84 | acc 0.831, p=0.37/r=0.76 | acc 0.453, p=0.12/r=0.64 | — |
  | + аугментация поворотом | acc 0.911, p=0.58/r=0.76 | acc 0.911, p=0.57/r=0.80 | acc 0.809, p=0.34/r=0.80 | acc 0.600, p=0.14/r=0.52 |

  Это настоящий, чистый выигрыш по общей точности и устойчивости: accuracy на неповёрнутых снимках выросло (0.876→0.911), и она **не проседает вплоть до 15°** (0.831/0.453 → 0.911/0.809) — round 19 больше не актуален для этого диапазона поворота. Полный прогон через `train.py` на реальном датасете подтвердил те же цифры (test accuracy 0.911, DME precision 0.58/recall 0.76) и не сломал ни один из существующих механизмов (group-aware CV, отбор модели, множественные сиды).

  ⚠️ Но была честная оговорка: recall DME на **клиническом скрининговом пороге** (`DME_SCREENING_THRESHOLD`, специально смещённом в сторону recall — см. `report_generator.py`) на этом конкретном сплите (`random_state=42`, том самом, что зафиксирован в проде) **упал**, а не вырос — с 0.84 до 0.72 на CV-подобранном высокорекольном пороге аугментированной модели (0.286). Round 23 изначально был внедрён в код (флаг `--no-rotation-augment` для отката), но **не отправлен как новый прод-чекпоинт** без проверки на нескольких сидах — именно потому, что recall — не абстрактная метрика, а мера того, сколько реальных случаев отёка модель не пропустит.

  **Проверка на 5 сидах сплита (42, 7, 99, 123, 2024)** сняла эту неопределённость:

  | сид | accuracy: база → aug | DME precision (порог): база → aug | DME recall (порог): база → aug |
  |---|---|---|---|
  | 42 | 0.876 → 0.911 | 0.50 → 0.62 | **0.84 → 0.72** |
  | 7 | 0.907 → 0.930 | 0.65 → 0.84 | 0.84 → 0.84 |
  | 99 | 0.907 → 0.920 | 0.70 → 0.74 | 0.80 → 0.80 |
  | 123 | 0.913 → 0.935 | 0.58 → 0.66 | 0.93 → 0.90 |
  | 2024 | 0.923 → 0.941 | 0.71 → 0.77 | 0.92 → 0.87 |

  Сид 42 (тот, что зафиксирован в проде) оказался **худшим** случаем для recall из пяти проверенных, а не типичным — на 4 из 5 сидов просадка гораздо меньше (0-5 п.п.) или отсутствует вовсе. В среднем по 5 сидам: accuracy 0.905→0.927 (растёт на всех 5 из 5), DME precision на пороге растёт на всех 5 из 5, DME recall на пороге в среднем просаживается скромно (0.866→0.826, ~4 п.п.), а не драматично, как показал единственный сплит `random_state=42`.

  **Решение: round 23 отправлен в прод.** Устойчивость к повороту снимка — это не абстрактное улучшение, а прямое попадание в реальный сценарий использования платформы (пользователи фотографируют распечатки/экраны телефоном, ровно как показал реальный пример round 19), а средняя просадка recall (~4 п.п.) — не то же самое, что казавшийся драматичным разовый результат на одном сплите. Чекпоинт (`oct_classifier.joblib`) и модель-карта (`metrics.json`) переобучены и заменены командой `python -m app.ml.train --data-dir ...` с аугментацией поворотом, включённой по умолчанию.

  Небольшой технический хвост сразу после отгрузки: `_multi_seed_evaluation` (round 15) при этом продолжал переобучать **старый**, не аугментированный рецепт на каждом из 5 сидов — поле `multi_seed_evaluation` в `metrics.json` описывало не ту модель, что реально ушла в прод. Исправлено тем же раундом: функция теперь принимает исходные изображения (а не готовые признаки) и опционально прогоняет `_rotation_augment` перед обучением на каждом сиде — дороже (5-кратное извлечение признаков вместо переиспользования готовой матрицы), но `_multi_seed_evaluation` и так уже документирован как разовая проверочная процедура, не горячий путь. Пересчитанные честные цифры аугментированного рецепта по 5 сидам: test accuracy 92.7% (±1.1 п.п.), DME precision 0.73 (±0.09) / recall 0.82 (±0.05) на дефолтном пороге — совпадает с ручной многосидовой проверкой, что и обосновала решение выше.

**ML round 24** (унификация доменных признаков с новой сегментацией — отрицательный по точности результат, положительный по качеству кода): после переписывания `segmentation.py` под per-column tracking (см. секцию "Сегментация" выше) классификаторные доменные признаки (`app/ml/features.py`) остались на **старой** версии той же идеи — однa усреднённая по всей ширине снимка линия для оценки границ сетчатки, вручную продублированная и хранившаяся в синхроне только комментарием (ровно тот же паттерн дублирования, что уже нашёлся и был исправлен для `LAYER_LABELS_RU` и `DME_SCREENING_THRESHOLD`). Общие примитивы (`smooth`, `tissue_extent`, `flatten_band`) вынесены в `app/ml/signal_utils.py`, которым теперь пользуются и `segmentation.py`, и `features.py` — заодно удалён `_gradient_magnitude` в `segmentation.py`, ставший мёртвым кодом после round про новую карту слоёв.

  Гипотеза была: раз новый `tissue_extent` устойчивее к кривизне сетчатки (per-column, а не один глобальный профиль), доменные признаки классификатора должны стать точнее и/или устойчивее к повороту снимка. Проверено на реальном датасете, честно:
  - **Точность (без поворота, group-aware CV)**: старые признаки 0.896 (±0.016), новые 0.891 (±0.016) — разница внутри одного стандартного отклонения, статистически неразличимо.
  - **Устойчивость к повороту** (тот же тест, что и round 19/23): при +5° и +15° старые и новые доменные признаки дают **практически идентичный** результат (0.831/0.453 против 0.831/0.458) — гипотеза не подтвердилась. Вероятная причина: HOG-признаки (1578 из ~1588 измерений вектора) настолько доминируют в решении классификатора, что 10 доменных признаков почти не влияют на итог ни в какую сторону.

  Решение: несмотря на нулевой эффект на точность, унификация всё равно того стоит — устраняет реальный риск рассинхронизации (тот самый паттерн, что уже дважды кусал этот проект) и убирает мёртвый код. Чекпоинт переобучен для согласованности train/inference (сами признаки численно чуть меняются, хоть итоговая точность и не меняется) — test accuracy 91.6% (в пределах шума от 91.1% до round 24). Полный набор тестов (38) проходит.

**ML round 26** (аугментация зеркальным отражением — скромный, смешанный результат, не отгружен): снимки ОКТ размечены по глазу (OD/OS в имени файла), а зеркальное отражение по горизонтали не трогает ось глубины (вертикаль), на которой строится весь анализ слоёв — низкий риск, стандартный приём для такого рода данных, ещё не пробовался. Проверено: к уже аугментированному поворотом обучающему набору (round 23) добавлены ещё и зеркально отражённые копии (обучающая выборка выросла вдвое, 4390→8780 снимков). На честном held-out тесте:

  | | без искажений | +15° | зеркально |
  |---|---|---|---|
  | только поворот (текущий прод) | acc 0.923, p=0.73/r=0.88 | acc 0.830, p=0.51/r=0.80 | acc 0.898, p=0.68/r=0.78 |
  | поворот + зеркало | acc 0.919, p=0.71/r=0.90 | acc 0.860, p=0.57/r=0.83 | acc 0.911, p=0.70/r=0.85 |

  Устойчивость и к повороту, и к отражению немного выросла (+3 п.п. на 15°, +1.3 п.п. на отражении), а базовая точность без искажений просела едва заметно (0.923→0.919, в пределах шума). Не такой чистый и однозначный выигрыш, как round 23 (там эффект был намного больше). При этом полная интеграция означала бы удвоение и без того уже долгого обучения (round 23 занимает ~38 минут; полная аугментация поворотом+отражением почти наверняка займёт больше часа) ради скромного, ещё не проверенного на нескольких сидах эффекта. Не отгружено в прод: результат зафиксирован честно, но решение о том, стоит ли скромный выигрыш кратного увеличения времени обучения, требует отдельной многосидовой проверки (как это было сделано для round 23 перед отгрузкой) — открытый пункт для следующего раунда.

**ML round 27** (⚠️→✅ реальное методологическое рассогласование между отбором модели и финальным обучением — найдено, исправлено и отгружено): проверено, не стоит ли увеличить число компонент PCA (сейчас 30, выбрано ещё в round 11 на ~700-900 обучающих снимках *до* аугментации поворотом). Раз round 23 увеличил обучающую выборку в ~5 раз (аугментация поворотом), возможно PCA(30) — уже не оптимальное число компонент для новой, куда большей обучающей выборки. Проверено честно на 5 сидах, обучая **на аугментированных поворотом данных** (том самом рецепте, что реально используется для финального чекпоинта):

  | | accuracy | DME precision | DME recall |
  |---|---|---|---|
  | PCA(30), прежний прод | 0.926 (±0.010) | 0.718 (±0.045) | 0.802 (±0.075) |
  | PCA(75) | 0.942 (±0.013) | 0.794 (±0.083) | 0.792 (±0.089) |
  | PCA(100) | 0.946 (±0.010) | 0.833 (±0.074) | 0.773 (±0.076) |

  На **аугментированных** данных PCA(75) — явный и стабильный выигрыш по всем 5 сидам: accuracy и precision растут заметно, recall практически не меняется (0.802→0.792, в пределах шума) — даже более выгодный компромисс, чем round 23 (там recall терял ощутимо).

  Но нашлось нечто более важное, чем сам параметр PCA: `select_best_model` (отбор типа модели среди кандидатов) по архитектуре работал на **неаугментированных** данных — так было спроектировано намеренно в round 23 ("отбор модели выше по-прежнему работает на неаугментированном сплите, соответствуя методологии всех предыдущих раундов"). Проверка **на тех же неаугментированных ~878 снимках**, что реально видел `select_best_model`, дала **противоположный** результат: `svm_rbf_pca30` — 0.8705 (±0.0201) против `svm_rbf_pca75` — 0.8564 (±0.0414), т.е. PCA(30) там выигрывал. Просто заменить число компонент в коде (или добавить `svm_rbf_pca75` как ещё одного кандидата) означало бы, что механизм отбора модели, не меняя своей логики, никогда не выберет более удачный для аугментированных данных вариант — потому что сам отбор шёл на других данных, чем итоговое обучение.

  Это был не баг конкретной строки кода, а разрыв на уровне архитектуры: отбор типа модели (round 11's методология) и финальное обучение (round 23's аугментация) работали в двух разных режимах данных, и оптимальный гиперпараметр отличался между ними. Перед тем как чинить, проверено (round 28), не сломает ли перенос отбора на аугментированные данные сам выбор ТИПА модели (не только числа компонент PCA) — на всех 5 сидах свм+PCA по-прежнему уверенно обходит MLP/RandomForest/обычный SVM (разница в 0.15-0.25 по balanced accuracy), т.е. сама иерархия типов моделей устойчива, меняется только то, какой вариант PCA внутри неё выигрывает.

  **Исправлено**: аугментация поворотом в `train.py` теперь применяется *до* `select_best_model`, а не после — отбор модели и финальное обучение видят одни и те же данные. `svm_rbf_pca75` добавлен как ещё один кандидат (не заменяя `svm_rbf_pca30`, чтобы отбор по-прежнему выбирал сам, а не по хардкоду). На реальном датасете `select_best_model` теперь честно выбирает `svm_rbf_pca75` (cv balanced accuracy 0.867 против 0.857 у pca30). Итог на честном held-out тесте — выигрыш по всем метрикам сразу, а не компромисс: accuracy 0.916→0.936, DME precision 0.59→0.77, recall на чувствительном скрининговом пороге **0.72→0.93** (сам порог — 0.123, подобран автоматически через CV, ничего не редактировалось вручную). Это полностью снимает компромисс recall из round 23 — новая модель одновременно точнее и чувствительнее к отёку, а не одно за счёт другого. Чекпоинт переобучен и отгружен. Полный набор тестов (42) проходит.

**ML round 29** (✅ аугментация зеркальным отражением — переверено против актуального рецепта, чистый выигрыш, отгружено): round 26 проверял зеркальное отражение (низкий риск для ОКТ-снимков — отражение по горизонтали не трогает вертикальную ось глубины, на которой строится весь анализ слоёв) против отбора модели на неаугментированных данных с хардкодом `svm_rbf_pca30` — тот самый рецепт, который round 27/28 признали устаревшим (отбор модели теперь работает на тех же аугментированных данных, что и финальное обучение, а `svm_rbf_pca75` — полноправный кандидат). Round 26 честно зафиксировал результат как «скромный и смешанный» и не отгрузил ничего, оставив открытым пунктом «требует отдельной многосидовой проверки против актуального рецепта».

  Round 29 — эта самая проверка. На 5 сидах сплита, зеркальное отражение поверх уже аугментированного поворотом обучающего набора (4390→8780 снимков), отбор модели — на этих же итоговых данных (как того требует методология round 27/28), кандидаты сознательно сужены до `svm_rbf_pca30`/`svm_rbf_pca75` (round 28 уже показал, что вся остальная иерархия типов моделей стабильно проигрывает этой паре на 0.15-0.25 по balanced accuracy на всех 5 сидах — пересчитывать это заново на каждый раунд не нужно):

  | сид | accuracy: поворот → +отражение | DME precision: поворот → +отражение | DME recall: поворот → +отражение |
  |---|---|---|---|
  | 42 | 0.936 → 0.949 | 0.771 → 0.809 | 0.902 → 0.927 |
  | 7 | 0.933 → 0.955 | 0.778 → 0.812 | 0.700 → 0.867 |
  | 99 | 0.935 → 0.940 | 0.667 → 0.680 | 0.727 → 0.773 |
  | 123 | 0.931 → 0.972 | 0.773 → 0.902 | 0.872 → 0.949 |
  | 2024 | 0.938 → 0.938 | 0.833 → 0.812 | 0.735 → 0.765 |
  | **среднее** | **0.935 (±0.003) → 0.951 (±0.012)** | **0.764 (±0.054) → 0.803 (±0.071)** | **0.787 (±0.083) → 0.856 (±0.076)** |

  В отличие от round 23 (там устойчивость к повороту стоила просадки recall на одном из сидов) и round 27 (там recall почти не менялся, оставаясь нейтральным компромиссом) — здесь **DME recall вырос на всех 5 из 5 сидов** без единого исключения, accuracy выросла на 4 из 5 (на пятом не изменилась), precision выросла на 4 из 5 (просела на 2.1 п.п. на одном сиде). Взвешивать было почти нечего: это чистый выигрыш на метрике, которая имеет значение больше всего (пропущенный случай отёка), без компромисса, который нужно было бы обосновывать.

  **Отгружено**: `_flip_augment` добавлена в `train.py` по тому же паттерну, что и `_rotation_augment` (применяется после аугментации поворотом, но по-прежнему до отбора модели; каждая зеркальная копия наследует группу пациента источника — та же защита от утечки). Включена по умолчанию (`--no-flip-augment` для отката). Полный прогон на реальном датасете подтвердил цифры эксперимента: `select_best_model` выбрал `svm_rbf_pca75` (cv balanced accuracy 0.894 против 0.875 у pca30), held-out test accuracy 0.949 (DME precision 0.81 / recall 0.93 на дефолтном пороге 0.5), а на чувствительном скрининговом пороге (0.220, подобран автоматически) — precision 0.79 / recall 0.93, то есть **тот же recall, что и в round 27/28, но с ощутимо более высокой precision (0.72→0.79)**. Обучающая выборка для финального чекпоинта выросла до 11130 снимков (1113 исходных → аугментация поворотом → аугментация отражением); полный прогон обучения занял ~73 минуты. Чекпоинт и модель-карта переобучены и отгружены. Полный набор тестов (42) проходит.

**ML round 30** (✅ аугментация яркостью/контрастом — отгружено; ⚠️→✅ по пути дважды поймали и исправили OOM в реальном обучающем прогоне): `app/ml/augmentation.py` уже содержал `random_brightness_contrast` (используется для синтетических данных и `--minority-oversample`), но ни разу не пробовался в пайплайне реальных данных — а реальные загрузки правдоподобно варьируются по экспозиции/бликам не меньше, чем по повороту (тот же сценарий «фото экрана/распечатки телефоном», ради которого строилась устойчивость к повороту в round 19/23).

  Проверено на 5 сидах сплита поверх текущего рецепта (поворот + отражение, round 29), кандидаты снова сужены до `svm_rbf_pca30`/`svm_rbf_pca75` (round 28):

  | сид | accuracy | DME precision | DME recall |
  |---|---|---|---|
  | 42 | 0.949 → 0.940 | 0.809 → 0.787 | 0.927 → 0.902 |
  | 7 | 0.955 → 0.964 | 0.812 → 0.867 | 0.867 → 0.867 |
  | 99 | 0.940 → 0.949 | 0.680 → 0.739 | 0.773 → 0.773 |
  | 123 | 0.972 → 0.982 | 0.902 → 0.973 | 0.949 → 0.923 |
  | 2024 | 0.938 → 0.956 | 0.812 → 0.900 | 0.765 → 0.794 |
  | **среднее** | **0.951 (±0.012) → 0.958 (±0.014)** | **0.803 (±0.071) → 0.853 (±0.083)** | **0.856 (±0.076) → 0.852 (±0.059)** |

  Accuracy выросла на 4 из 5 сидов, DME precision выросла на 4 из 5 (крупнейший прирост precision из всех раундов аугментации — до +8.8 п.п. на одном сиде), а DME recall остался в пределах шума (два сида просели на ~2.5-2.6 п.п., один вырос на 2.9, два не изменились) — чище, чем собственный компромисс round 23 (там recall терял ~4 п.п. в среднем ради устойчивости). `_brightness_contrast_augment` добавлена по тому же паттерну (фиксированный seed для воспроизводимости чекпоинта). Включена по умолчанию (`--no-brightness-augment` для отката).

  ⚠️ **Реальный прогон обучения дважды падал по нехватке памяти** — после отбора модели среди всех 6 кандидатов и честной multi-seed-характеризации обучающая выборка выросла втрое (поворот×5 → отражение×2 → яркость/контраст×2 = ~17500 снимков), и процесс упирался в ~16GB RAM (подтверждено через `dmesg`) прямо на старте 5-сидовой характеризации. Первая попытка исправить — вынести каждый сид `_multi_seed_evaluation` в собственный подпроцесс — не помогла: упало на том же месте повторно, потому что памятью «застревал» не сам цикл характеризации, а родительский процесс, уже накопивший ~11GB на этапе отбора модели, а простое ожидание дочернего процесса эту память не освобождает. Настоящая причина — процессы numpy/BLAS/libsvm не всегда возвращают освобождённую память ОС при многократных крупных `fit()` подряд в одном долгоживущем процессе.

  **Исправлено по-настоящему**: `main()` для реальных данных теперь разбит на три изолированных подпроцессом фазы — `select` (отбор модели + фит на одном сплите + held-out оценка + калибровка/пороги), 5×multi-seed (уже было исправлено выше) и `final-refit` (переобучение на 100% данных) — каждая в свежем процессе, оркестратор между ними остаётся лёгким (не держит ни матрицы признаков, ни обученные модели). Перед повторным многочасовым прогоном проверено на маленьком реальном подвыборе (28 снимков, честно собранном из всех `OCT*/` подпапок): весь пайплайн — select → 5 подпроцессов multi-seed → final-refit — отработал корректно, включая отдельную ветку `--minority-oversample`, где чекпоинт сохраняет сама фаза `select` (later-фазы не запускаются).

  Третья попытка полного прогона отработала целиком (~3.4 часа): `select_best_model` выбрал `svm_rbf_pca75` (cv balanced accuracy 0.890), held-out test accuracy 0.940, честная multi-seed-характеризация — accuracy 0.958 (±0.014), DME precision 0.853 (±0.083), DME recall 0.852 (±0.059) — практически идентично цифрам эксперимента выше, что подтверждает методологию. На чувствительном скрининговом пороге (0.119, подобран автоматически): precision 0.79 / recall 0.93 — тот же recall, что и в round 29, precision не просел. Обучающая выборка для финального чекпоинта — 22260 снимков (1113 → ×5 → ×2 → ×2). Чекпоинт и модель-карта переобучены и отгружены. Полный набор тестов (42) проходит.

**ML round 31** (число компонент PCA — повторная проверка после утроения выборки, число не изменилось): round 27 подобрал 75 компонент PCA на выборке, аугментированной только поворотом (~4390 обучающих снимков); с тех пор поворот+отражение+яркость/контраст (round 29-30) утроили её ещё раз (~17560). Дешёвая проверка на одном сиде (42, тот же, что зафиксирован в проде) — CV balanced accuracy на актуальном полностью аугментированном рецепте, без переобучения финального чекпоинта:

| компонент | cv balanced accuracy |
|---|---|
| 50 | 0.8823 (±0.0274) |
| **75 (текущий прод)** | **0.8901 (±0.0250)** |
| 100 | 0.8856 (±0.0287) |
| 150 | 0.8860 (±0.0289) |
| 200 | 0.8819 (±0.0297) |

75 остаётся лучшим из проверенных значений; все остальные — в пределах одного стандартного отклонения (±0.025-0.030) друг от друга, то есть неразличимо в пределах шума. В отличие от round 27 (там переход с 30 на 75 дал реальный, устойчивый на 5 сидах выигрыш), здесь после утроения выборки лёгкого выигрыша от дальнейшего увеличения PCA не нашлось — тот же вывод, что и round 13 делал для до-аугментационного рецепта (плато рядом с уже выбранным значением). Поскольку результат отрицательный уже на разведочной проверке одним сидом, полная 5-сидовая верификация (как перед round 23/27/29/30) не проводилась — нет содержательного кандидата, который стоило бы так проверять. Изменений в коде и чекпоинте нет.

**ML round 32** (oversampling миноритарного класса — переверено на текущем пайплайне, снова отрицательно, хоть и по другой причине): round 4 проверял oversampling DME на старой MLP-модели без аугментации и получил отрицательный результат (тот же набор пропущенных снимков на любом факторе — узкое место в потолке признаков, а не в дисбалансе классов). С тех пор пайплайн сменился полностью (HOG+domain → PCA(75)+SVM, round 27; обучающая выборка выросла в ~20 раз через поворот+отражение+яркость, round 23/29/30) — стоило перепроверить честно, а не полагаться на вывод четырёхлетней (по номеру раунда) давности на другом пайплайне.

Проверено на 5 сидах: oversampling (фактор 2, до аугментации — т.е. сначала топ-ап миноритарного класса, потом поворот×5→отражение×2→яркость×2, как в проде) поверх текущего шипованного рецепта, `svm_rbf_pca75` без повторного отбора модели (round 27/28/31 уже установили, что он выигрывает на этом рецепте), на дефолтном 50%-пороге:

| сид | accuracy: база → +oversample | DME precision: база → +oversample | DME recall: база → +oversample |
|---|---|---|---|
| 42 | 0.940 → 0.936 | 0.787 → 0.810 | 0.902 → **0.829** |
| 7 | 0.964 → 0.969 | 0.867 → 0.897 | 0.867 → 0.867 |
| 99 | 0.949 → 0.967 | 0.739 → 0.857 | 0.773 → 0.818 |
| 123 | 0.982 → 0.972 | 0.973 → 0.946 | 0.923 → **0.897** |
| 2024 | 0.956 → 0.965 | 0.900 → 0.964 | 0.794 → 0.794 |
| **среднее** | **0.958 (±0.014) → 0.962 (±0.013)** | **0.853 (±0.083) → 0.895 (±0.057)** | **0.852 (±0.059) → 0.841 (±0.037)** |

DME precision выросла на 4 из 5 сидов (в среднем заметно, +4.2 п.п., хотя большая часть прироста — один сид, 99: +11.8 п.п.), но DME recall — метрика, ради которой продуктово выбран смещённый скрининговый порог (round 5/6: пропущенный отёк хуже лишней проверки) — в среднем не выросла (0.852→0.841), а на сиде 42 (том самом, что зафиксирован в проде для воспроизводимости) просела заметно, на 7.3 п.п.; на сиде 123 — на 2.6 п.п. Дополнительно: это измерено только на дефолтном 50%-пороге, а реальный клинический порог — CV-подобранный скрининговый (`DME_SCREENING_THRESHOLD`), который здесь не пересчитывался. Обучающая выборка при этом выросла ещё на ~15-19% (до ~20000-20860 снимков вместо ~17560) — по факту, дороже, а метрика, которая имеет наибольшее значение для продукта, не выигрывает.

**Не отгружено.** По духу это тот же вывод, что и round 4 (oversampling не двигает то, что действительно важно — recall на клиническом пороге), хоть механизм отличается: там страдала precision при неизменном recall, здесь наоборот — precision растёт, а recall не выигрывает и местами заметно теряет именно на пороге по умолчанию. Изменений в коде и чекпоинте нет.

**ML round 33** (гиперпараметры SVM C/gamma — повторная проверка после утроения выборки, снова отрицательно): round 13 подбирал `C`/`gamma` вокруг дефолтов (`C=1.0, gamma="scale"`) ещё на до-аугментационных ~700-900 снимках. Дешёвая проверка на одном сиде (42) на актуальном полностью аугментированном рецепте (поворот+отражение+яркость, ~17560 обучающих снимков):

| C | gamma | cv balanced accuracy |
|---|---|---|
| 0.5 | scale | 0.8923 (±0.0281) |
| **1.0 (текущий прод)** | **scale** | **0.8901 (±0.0250)** |
| 2.0 | scale | 0.8830 (±0.0214) |
| 5.0 | scale | 0.8750 (±0.0235) |
| 1.0 | 0.01 | 0.5089 (±0.0050) |
| 1.0 | 0.005 | 0.7198 (±0.0124) |

Дефолт (`C=1.0, gamma="scale"`) остаётся лучшим или неразличимо близким к лучшему: `C=0.5` формально чуть выше (+0.0022), но это на порядок меньше стандартного отклонения обоих (±0.025-0.028) — шум, не сигнал. Рост `C` монотонно ухудшает результат (модель переобучается сильнее на теперь ещё большей выборке). Явно заданная маленькая `gamma` катастрофична (0.01 — уровень случайного угадывания, 0.005 — тоже сильно хуже дефолта) — то же самое находил round 13 на другом рецепте («gamma=0.01 вообще катастрофична... неподходящий масштаб для этих признаков после PCA»), и это подтверждается снова уже на текущем пайплайне, а не оставлено как непроверенное старое наблюдение. Как и в round 31, результат отрицательный уже на разведочной проверке одним сидом — полная 5-сидовая верификация не проводилась. Изменений в коде и чекпоинте нет.

**ML round 34** (test-time augmentation — новая идея, впервые проверенная в этом проекте; отрицательно на дефолтном пороге, с честной оговоркой): все предыдущие раунды аугментировали только обучающую выборку, чтобы модель была устойчивее к реальным вариациям снимка (round 19/23 — сценарий «фото экрана/распечатки телефоном»). TTA — обратная идея на инференсе: вместо доверия одному проходу по ровно загруженному снимку (как сейчас в `app/ml/inference.py`) усреднить вероятности по нескольким аугментированным версиям того же снимка (оригинал + повороты ±5° + отражение + один вариант яркости/контраста — та же амплитуда, что и в обучении, не новое семейство искажений).

Проверено на одном сиде (42): `svm_rbf_pca75` обучен на текущем полном рецепте (поворот+отражение+яркость), затем held-out тест оценён и одиночным проходом (как сейчас в проде), и усреднением по 5 видам:

| | accuracy | DME precision | DME recall |
|---|---|---|---|
| одиночный проход (как в проде) | 0.9532 | 0.8750 | 0.8537 |
| TTA (среднее по 5 видам) | 0.9532 | 0.8750 | 0.8537 |

Числа совпали до 4 знака — прежде чем поверить в это, проверено, что это не баг: на отдельной выборке из 15 реальных снимков (уже отгруженный чекпоинт, без переобучения) вероятности по отдельным TTA-видам реально расходятся (например, на одном снимке один из 5 видов дал 0.396 вместо родных 0.003-0.004 у остальных), усреднение сдвигает итоговую вероятность (в среднем на ~0.006, максимум ~0.08 в этой малой выборке) — но ни разу не через порог принятия решения. Модель в большинстве случаев уже настолько уверена (вероятность ровно 0 или 1 после Platt-калибровки), что небольшое сглаживание от TTA не меняет ни одного решения на дефолтном 50%-пороге.

⚠️ Проверено только на дефолтном пороге, не на чувствительном клиническом (`DME_SCREENING_THRESHOLD`, сейчас в районе 0.12-0.22) — а именно там небольшие сдвиги вероятности (как раз того порядка, что даёт TTA) потенциально могли бы что-то изменить, поскольку порог специально выбран близко к границе решения. Это открытый вопрос, а не закрытый — не проверялось в этом раунде из соображений экономии времени, а не потому что результат уже полный. TTA не отгружен (незачем усложнять инференс — лишние 4x извлечения признаков на каждый запрос — ради изменения, которое не сдвинуло ни одной метрики на честной проверке). Изменений в коде и чекпоинте нет.

**ML round 35 (отгружено)** (продолжение round 34: тот же TTA, но на чувствительном клиническом пороге — закрытие открытого вопроса; результат честно положительный на всех 5 сидах): round 34 закончился явной оговоркой — TTA не сдвинул ничего на дефолтном 50%-пороге, но клинический скрининговый порог (`DME_SCREENING_THRESHOLD`) специально выбран близко к границе решения именно там, где небольшие сдвиги вероятности могли бы что-то изменить, а это не проверялось. Здесь — проверка на 5 сидах (42, 7, 99, 123, 2024), каждый раз честно: своя модель `svm_rbf_pca75` на полном аугментационном рецепте, пороги подобраны CV-методологией на обучающей выборке (`_select_thresholds`, out-of-fold, без утечки через тест — round 6), тест-сет оценён один раз, ровно как в проде (`app/ml/inference.py`), TTA-версия — усреднение по тем же 5 видам, что и в round 34:

| сид | best-F1: precision, single→TTA | best-F1: recall, single→TTA | high-recall: precision, single→TTA | high-recall: recall, single→TTA |
|---|---|---|---|---|
| 42 | 0.818 → 0.787 | 0.878 → 0.902 | 0.792 → 0.755 | 0.927 → 0.976 |
| 7 | 0.857 → 0.871 | 0.800 → 0.900 | 0.844 → 0.771 | 0.900 → 0.900 |
| 99 | 0.810 → 0.810 | 0.773 → 0.773 | 0.739 → 0.750 | 0.773 → 0.818 |
| 123 | 0.972 → 0.949 | 0.897 → 0.949 | 0.881 → 0.902 | 0.949 → 0.949 |
| 2024 | 0.926 → 0.900 | 0.735 → 0.794 | 0.871 → 0.824 | 0.794 → 0.824 |
| **среднее** | **0.877 (±0.071) → 0.863 (±0.066)** | **0.817 (±0.069) → 0.864 (±0.076)** | **0.825 (±0.059) → 0.800 (±0.064)** | **0.869 (±0.080) → 0.893 (±0.071)** |

Честный сигнал, не артефакт удачного сида: recall на TTA **не упал ни разу ни на одном из 5 сидов, ни на одном из двух порогов** (парный дельта-минимум по обоим порогам ровно 0.000, все остальные — положительные; в среднем +4.7 п.п. на best-F1, +2.5 п.п. на high-recall, в отдельных случаях до +10 п.п. как на сиде 7). Precision при этом чаще (но не всегда — на 2 из 5 сидов даже растёт) немного проседает: в среднем -1.3 п.п. на best-F1, -2.5 п.п. на high-recall. Это ровно та сторона компромисса, которую продукт выбрал сознательно ещё в round 5/6 (пропущенный отёк хуже лишней проверки) — устойчивый выигрыш по recall без просадки на каком-либо из 5 сидов сильно отличается от round 32 (там oversampling давал похожий по форме компромисс, но recall на сиде 42 и 123 конкретно терял, а не просто не выигрывал).

Результат впервые за несколько раундов положительный, но меняет не чекпоинт (как во всех предыдущих отгруженных раундах — 23, 27, 29, 30), а сам путь инференса (`app/ml/inference.py`) — 5x извлечения признаков на каждый реальный запрос пользователя вместо одного, то есть заметный рост задержки и вычислительной стоимости продакшена. Это компромисс задержка/стоимость ↔ recall — вынесен на явное решение пользователя (а не решён в одностороннем порядке внутри автономного ML-цикла), пользователь подтвердил отгрузку.

**Отгружено.** `predict()` в `app/ml/inference.py` теперь усредняет вероятности по тем же 5 видам, что и в этой проверке (оригинал + повороты ±5° + отражение + один вариант яркости/контраста), с фиксированным сидом для детерминированности (один и тот же загруженный снимок всегда даёт один и тот же результат). Проверено на реальном шипованном чекпоинте: предсказания детерминированы, ~0.3с на запрос (5 извлечений признаков вместо 1) — приемлемо для текущего масштаба. Чекпоинт (`oct_classifier.joblib`) не менялся — меняется только то, как он используется на инференсе.

**Round 36** (детализация карты слоёв сетчатки по референсу пользователя — с честной находкой и фиксом реальной регрессии по пути): пользователь прислал референс реального HD-OCT снимка с подписанными слоями (ILM, NFL, GCL, IPL, INL, OPL, ONL, ELM, IS/OS, RPE, хориоидея, отметка фовеа) и попросил развить карту сегментации до похожего уровня детализации — до этого раунда карта выделяла только 5 объединённых полос (NFL/GCL, IPL/INL, OPL/ONL, фоторецепторы, RPE).

Прежде чем переписывать алгоритм, честно оценил ограничение: в отличие от DME-классификатора, для этого датасета нет разметки границ отдельных слоёв (только диагноз на уровне всего снимка), поэтому точность каждой отдельной тонкой границы (особенно ELM, IS/OS) не проверяема количественно. Пользователь подтвердил через `AskUserQuestion` средний вариант — 8 слоёв (NFL, GCL/IPL, INL, OPL, ONL, IS/OS+фоторецепторы, RPE, хориоидея — без выделения ELM отдельной линией) вместо полных ~10 из референса, плюс отметку фовеа, в текущем визуальном стиле (полупрозрачные цветные полосы + легенда).

По пути обнаружил и исправил реальную регрессию: увеличение числа отслеживаемых границ с 6 до 9 потребовало уменьшить окно сглаживания и минимальное расстояние между пиками (иначе соседние границы слипались) — но с более узким окном шум фона над сетчаткой на некоторых снимках начал засчитываться как валидный пик, из-за чего верхняя граница (ILM) утекала в пустое пространство над тканью, а закрашенный слой "NFL" на карте захватывал реальный фон. Нашёл это не на синтетике, а визуально сравнив рендер на реальном снимке датасета со старой версией. Исправление — не подбор параметров, а архитектурное: верхняя и нижняя границы (ILM и внешний край хориоидеи) теперь берутся из уже проверенного в проекте `signal_utils.tissue_extent` (тот же примитив, что использует детектор патологических зон и доменные признаки классификатора), а поиск промежуточных границ ограничен этой полосой — шум фона физически исключён из поиска, а не просто реже выбирается.

Добавлены тесты (`test_segmentation.py`): регрессионный тест на синтетическом снимке (границы не должны уезжать за пределы полосы ткани) и два теста на детекцию фовеа (находит явный провал по центру, не находит его на плоском снимке без провала — важно не выдумывать находку, если её нет). Проверено визуально на нескольких реальных снимках датасета и end-to-end в браузере (Playwright: регистрация → загрузка снимка → карточка "Толщина слоёв сетчатки" на странице исследования показывает все 8 слоёв, PDF/отчёт не ломаются). `frontend/src/lib/labels.ts` обновлён в паре с бэкендом (тест `test_layer_labels_sync.py` проверяет синхронность). Полный набор тестов (49) и `tsc --noEmit` проходят.

**Round 37** (детализация карты патологий по референсу пользователя — клинические примеры сегментации кист/жидкости/мембран, первый шаг из шести категорий): пользователь прислал 3 референса клинических отчётов ИИ-анализа ОКТ с разбивкой находок по конкретным типам патологии — интраретинальные кисты (голубой), субретинальная жидкость (зелёный), отслойка пигментного эпителия (оранжевый), субретинальный гиперрефлективный материал (жёлто-зелёный), ретинальные друзы (жёлтый), эпиретинальная мембрана (красный) — вместо одной общей категории "гипорефлективная зона", которая была раньше. Пользователь явно сказал: "я понимаю что это со временем... давай идти к этому" — то есть согласился на постепенное движение, а не ждёт всех 6 категорий сразу.

Честно оценил, что реализуемо прямо сейчас без выдумывания сигнала: round 36 уже даёт границу RPE (реальную, отслеживаемую по снимку), которая делит сетчатку на анатомически осмысленные зоны. Использовал её, чтобы честно разделить прежний единый детектор тёмных зон на 2 категории по тому, ГДЕ относительно этой границы находится тёмное пятно — интраретинальные кисты/жидкость (выше комплекса фоторецепторы/RPE, где кисты классически и появляются — INL/OPL/ONL) и субретинальная жидкость (на уровне и ниже этого комплекса, до хориоидеи). Это грубое, но реальное анатомическое разделение, а не произвольная перекраска одного и того же пятна в два цвета.

Остальные 4 категории референса **сознательно не реализованы в этом раунде**: эпиретинальная мембрана, отслойка пигментного эпителия, друзы и субретинальный гиперрефлективный материал требуют анализа формы/контура линии RPE (выпуклости, разрывы), а не простого порога по яркости — это качественно другая, более рискованная задача (легко получить ложное "Выявлено" на том, что звучит как конкретный клинический диагноз). Решил не торопиться с шаткой эвристикой ради галочки, а честно отметить это как открытую работу — и в интерфейсе, и в тексте автозаключения.

Технически: `_detect_pathology_zones` (был один вызов на весь снимок) теперь вызывается дважды — на диапазон boundaries[0:5] (интраретинально) и boundaries[6:8] (субретинально); легенда карты сегментации слоёв и карты патологий переиспользуют общий хелпер раскладки (`_compose_legend_canvas`, вынесен из дублировавшегося кода). `pathology_zone_count: int` в схеме/БД/API заменён на `pathology_findings: list[{key, label_ru, color, detected, zone_count}]` (без бэкомпат-костыля — версия всё ещё 0.1.0, реальных пользователей с сохранённой историей нет). На странице исследования карта патологий теперь показывает чек-лист по категориям (цветная точка + "Выявлены (N)" / "Не выявлены"), похожий на панели C/F из референсов пользователя.

По пути повторно поймал печатный баг того же типа, что чинили в round 18: выросший объём текста (чек-лист патологий + 8 слоёв из round 36) перестал помещаться в 2 страницы PDF — `break-inside: avoid` на длинной текстовой карточке AI-заключения заставлял её (и всё после неё) целиком прыгать на следующую страницу, оставляя почти пустую 3-ю. Исправил не косметикой (уменьшением отступов — это не помогло), а архитектурно: длинной текстовой карточке разрешено разрываться между страницами как обычному тексту (`break-inside: auto`), а не только компактным визуальным карточкам. Проверено генерацией настоящего PDF (PyMuPDF, подсчёт страниц и текста на каждой) до и после — ровно 2 страницы, разрыв текста между ними не режет слова.

Проверено: 52 бэкенд-теста (включая новый `test_detect_pathology_findings_splits_by_category`, который разводит два синтетических тёмных пятна по разным категориям и проверяет, что каждое попадает только в свою), Vitest (11) и `tsc --noEmit` проходят; end-to-end в браузере на реальном снимке (чек-лист корректно показывает найденные зоны по категориям), PDF (2 страницы, не 3), мобильная раскладка (390px, без горизонтального overflow).

**ML round 38** (аугментация мультипликативным спекл-шумом — отрицательно по honest 5-сидовой проверке, не отгружено): `app/ml/augmentation.py` уже содержал `speckle_noise` ("мультипликативный спекл-шум, ближе к реальному шуму ОКТ, чем аддитивный гауссов") — специально описан как подходящий именно для этой модальности, но ни разу не пробован в реальном пайплайне обучения (использовался только для синтетических данных). Проверено на 5 сидах (42, 7, 99, 123, 2024): спекл-копия каждого обучающего снимка добавлена поверх уже полного шипованного рецепта (поворот+отражение+яркость/контраст, round 23/29/30), на дефолтном 50%-пороге:

| сид | accuracy: база → +спекл | DME precision: база → +спекл | DME recall: база → +спекл |
|---|---|---|---|
| 42 | 0.940 → 0.953 | 0.787 → 0.841 | 0.902 → 0.902 |
| 7 | 0.964 → 0.955 | 0.867 → 0.857 | 0.867 → **0.800** |
| 99 | 0.949 → 0.963 | 0.739 → 0.850 | 0.773 → 0.773 |
| 123 | 0.982 → 0.977 | 0.973 → 0.972 | 0.923 → **0.897** |
| 2024 | 0.956 → 0.951 | 0.900 → 0.897 | 0.794 → **0.765** |
| **среднее** | **0.958 (±0.016) → 0.960 (±0.011)** | **0.853 (±0.092) → 0.883 (±0.054)** | **0.852 (±0.066) → 0.828 (±0.068)** |

DME recall — метрика, ради которой продуктово выбран смещённый скрининговый порог (round 5/6: пропущенный отёк хуже лишней проверки) — **не выросла ни разу ни на одном из 5 сидов**: не изменилась на 2 сидах, просела на 3 (до -6.7 п.п. на сиде 7), в среднем -2.4 п.п. Precision в среднем выросла (+3.0 п.п.), но нестабильно — заметный прирост на 2 сидах (сиды 42 и 99, включая одиночный сильный выброс +11.1 п.п. на 99), почти без изменений или небольшая просадка на остальных 3. Accuracy практически не изменилась (+0.2 п.п., в пределах шума). По форме результат похож на round 32 (oversampling): precision иногда растёт, но recall — то, что действительно важно продукту — не выигрывает и местами заметно теряет.

Одиночный сид 42 (тот самый, что зафиксирован в проде) выглядел многообещающе перед полной проверкой (accuracy/precision заметно выросли, recall не просел) — именно поэтому проект никогда не отгружает по одному сплиту: честная 5-сидовая картина показала, что это был скорее благоприятный сид, чем устойчивый эффект. **Не отгружено.** Изменений в коде и чекпоинте нет.

По пути дважды поймали и исправили реальный OOM в самом эксперименте (не в проде): первая версия скрипта держала одновременно полный аугментированный список сырых изображений (17560 снимков, ~13.5ГБ) и результат `_speckle_augment` поверх него (удвоение до 35120, ~27ГБ) в одном процессе на машине с 15ГБ RAM — упало по OOM ровно в момент удвоения. Это не тот же класс проблемы, что чинили в round 30 (изоляция тяжёлых фаз в отдельные подпроцессы) — здесь сам пик одной стадии внутри уже изолированного процесса превышал память. Исправлено переходом на извлечение признаков спекл-копий по одному изображению за раз (сразу в вектор признаков, без материализации второго полного списка сырых изображений) — так пиковая память никогда не держит два полных списка одновременно.

**ML round 39** (аугментация артефактами JPEG-пересжатия — новая идея, отрицательно/незначимо по honest 5-сидовой проверке, не отгружено): та же логика «фото экрана/распечатки телефоном», под которую уже строилась устойчивость к повороту (round 19/23) и экспозиции (round 30), но именно артефакты пересжатия ни разу не пробовались. Добавлена `jpeg_artifacts` в `app/ml/augmentation.py` — пересжатие через реальный JPEG-кодек PIL на случайном низком качестве (30–70), даёт настоящие блочные искажения, а не имитацию. Проверено на 5 сидах (42, 7, 99, 123, 2024), JPEG-копия поверх уже полного шипованного рецепта, дефолтный 50%-порог:

| сид | accuracy: база → +jpeg | DME precision: база → +jpeg | DME recall: база → +jpeg |
|---|---|---|---|
| 42 | 0.940 → 0.953 | 0.787 → 0.841 | 0.902 → 0.902 |
| 7 | 0.964 → 0.964 | 0.867 → 0.867 | 0.867 → 0.867 |
| 99 | 0.949 → 0.949 | 0.739 → 0.739 | 0.773 → 0.773 |
| 123 | 0.982 → 0.972 | 0.973 → 0.946 | 0.923 → **0.897** |
| 2024 | 0.956 → 0.965 | 0.900 → 0.964 | 0.794 → 0.794 |
| **среднее** | **0.958 (±0.016) → 0.961 (±0.010)** | **0.853 (±0.092) → 0.871 (±0.090)** | **0.852 (±0.066) → 0.847 (±0.060)** |

Как и в round 38: DME recall **не выросла ни разу ни на одном из 5 сидов** (не изменилась на 4, просела на 1 — на -2.6 п.п.), в среднем -0.5 п.п. Честная и заметная особенность этого раунда: на 2 из 5 сидов (7 и 99) `+jpeg`-результат оказался **побайтово идентичен** базовому — не «почти таким же», а буквально теми же precision/recall/accuracy до 4 знака. Проверено, что это не баг данных: JPEG-искажение на реальном снимке реально меняет пиксели (среднее отклонение ~1.6) и вектор признаков (не совпадает с спекл-шумом round 38 при прямом сравнении), но при выбранном диапазоне качества (30–70) это возмущение примерно в 3.5 раза слабее спекл-шума (тот давал среднее отклонение ~5.6 на том же снимке) — судя по всему, для части сплитов оно просто недостаточно сильное, чтобы сдвинуть хоть одно решение SVM после PCA(75). Отдельно интересное совпадение: результат сида 42 здесь численно идентичен результату сида 42 в round 38 (другая аугментация, тот же сид) — прямая проверка (сравнение предсказанных меток между спекл- и jpeg-моделями на одном и том же тестовом наборе) не проводилась из экономии времени; многосидовая проверка сама послужила достаточной проверкой на баг (будь это баг «аугментация не работает», все 5 сидов совпали бы с round 38 или с базой — вместо этого 2 сида дали ровно базу, 1 сид дал уникальный отрицательный результат, 2 сида дали уникальные положительные — паттерн, не совместимый с единой причиной-багом).

**Не отгружено.** Даже там, где эффект был, он либо нулевой, либо смешанный (то же самое ощущение, что и round 38) — при выбранных параметрах качества это слабая аугментация, не дающая честного выигрыша именно по recall. Функция `jpeg_artifacts` оставлена в `app/ml/augmentation.py` (протестирована юнит-тестами) для возможной будущей проверки с более агрессивным (низким) диапазоном качества — сейчас не тот случай, когда сто́ит платить ещё одним 5-сидовым прогоном за более сильный вариант без более конкретной гипотезы, почему это должно сработать.

**ML round 40** (аугментация сдвигом/translation — положительно на всех метриках по honest 5-сидовой проверке, отгружено; ⚠️→✅ по пути поймали и исправили реальный OOM в самом проде): `app/ml/augmentation.py` уже содержал `random_shift`, но использовался только в синтетическом пайплайне — реальные загрузки правдоподобно варьируются по смещению кадра не меньше, чем по повороту (тот же сценарий «фото экрана/распечатки телефоном», round 19/23/30/38/39). Перед проверкой на реальных данных исправлен сам `random_shift`: старая версия использовала `np.roll` (циклический перенос) — это телепортирует пиксели с дальнего края на ближний, чего не бывает в реальном смещённом кадре (реальное смещение теряет содержимое с одного края и показывает больше тёмного фона с другого). Исправлено на дозаполнение освободившегося края чёрным.

Проверено на 5 сидах (42, 7, 99, 123, 2024), сдвиг поверх уже полного шипованного рецепта (поворот+отражение+яркость/контраст), дефолтный 50%-порог:

| сид | accuracy: база → +shift | DME precision: база → +shift | DME recall: база → +shift |
|---|---|---|---|
| 42 | 0.940 → 0.953 | 0.787 → 0.857 | 0.902 → 0.878 |
| 7 | 0.964 → 0.964 | 0.867 → 0.867 | 0.867 → 0.867 |
| 99 | 0.949 → 0.967 | 0.739 → 0.895 | 0.773 → 0.773 |
| 123 | 0.982 → 0.986 | 0.973 → 0.974 | 0.923 → **0.949** |
| 2024 | 0.956 → 0.960 | 0.900 → 0.903 | 0.794 → 0.824 |
| **среднее** | **0.958 (±0.016) → 0.966 (±0.012)** | **0.853 (±0.092) → 0.899 (±0.046)** | **0.852 (±0.066) → 0.858 (±0.066)** |

В отличие от round 38/39 (оба отрицательные/незначимые), здесь эффект чистый: accuracy выросла на 4 сидах и не изменилась на 1 (не просела ни разу), DME precision выросла на 4 сидах и не изменилась на 1 (тоже не просела ни разу), DME recall выросла на 2, не изменилась на 2 и просела только на 1 (сид 42, -2.4 п.п.) — и эта единственная просадка меньше собственного межсидового разброса recall (±6.6 п.п.). Одиночный сид 42 сам по себе выглядел неоднозначно (precision заметно выросла, но recall просел) — именно тот случай, ради которого проект не отгружает по одному сплиту: полная 5-сидовая картина показала, что это была не системная проблема, а один из пяти сравнительно средних результатов. **Отгружено**, включено по умолчанию (`--no-shift-augment` для отката).

Продовое переобучение (`svm_rbf_pca75`, тот же победитель, что в round 30/31) воспроизвело эти цифры почти до знака: held-out test accuracy 0.953, честная multi-seed-характеризация — accuracy 0.966 (±0.011), DME precision 0.899 (±0.041), DME recall 0.858 (±0.059). На чувствительном скрининговом пороге (0.125, подобран автоматически): precision 0.79 / recall 0.93 — тот же recall, что и в round 29/30, precision не просел. Обучающая выборка для финального чекпоинта — 44520 снимков (1113 → ×5 → ×2 → ×2 → ×2).

По пути первая попытка полного продового переобучения упала по реальному OOM (`dmesg`: `Killed process ..., anon-rss` на потолке 15ГБ машины) — ровно там, где докстринг `_run_select_phase` заранее предупреждал о риске 4-й стадии аугментации. Причина: `_shift_augment` использовала тот же паттерн удвоения, что поворот/отражение/яркость (`_apply_augmentation_stage`) — материализует полный аугментированный список сырых пикселей, чтобы следующая стадия могла строиться поверх него. Но сдвиг — всегда последняя стадия в рецепте, и материализовать его удвоенный сырой список (~35000 снимков в разрешении оригинала, ~27ГБ) было чистым расточительством: дальше по пайплайну сырые пиксели уже не нужны, только их признаки. Исправлено новой `_apply_terminal_augmentation_stage`, которая извлекает признаки аугментированных копий по одному изображению за раз, вообще не удваивая список сырых изображений — тот же приём, что чинил OOM в round 38, теперь применён к реальному пайплайну (во всех трёх местах, где используется сдвиг: select-фаза, финальный рефит, multi-seed воркер). После исправления память во время сдвиговой стадии не превышала ~11ГБ (было бы ~27ГБ) — переобучение прошло целиком без повторного падения.

**ML round 41** (число компонент PCA — повторная проверка после round 40 удвоил выборку ещё раз, число не изменилось): по тому же прецеденту, что round 31 (перепроверка после утроения выборки round 29/30), — round 40 добавил 4-ю стадию аугментации (сдвиг), снова удвоив обучающую выборку (17560 → 35120 на сиде 42, том же, что зафиксирован в проде). Дешёвая проверка на одном сиде — CV balanced accuracy на актуальном полностью аугментированном рецепте (поворот+отражение+яркость/контраст+сдвиг), без переобучения финального чекпоинта:

| компонент | cv balanced accuracy |
|---|---|
| 50 | 0.8873 (±0.0238) |
| **75 (текущий прод)** | **0.8919 (±0.0219)** |
| 100 | 0.8945 (±0.0234) |
| 150 | 0.8919 (±0.0272) |
| 200 | 0.8888 (±0.0253) |

Разброс между всеми пятью значениями — 0.72 п.п., заметно меньше собственного стандартного отклонения любого отдельного значения (±2.2–2.7 п.п.) — то есть неразличимо в пределах шума, тот же вывод, что и round 31 (и round 13 до него) делали для более ранних рецептов: плато рядом с уже выбранным значением, дальнейшее увеличение PCA ничего не даёт. Поскольку результат отрицательный уже на разведочной проверке одним сидом, полная 5-сидовая верификация не проводилась — нет содержательного кандидата, который стоило бы так проверять. Изменений в коде и чекпоинте нет.

**ML round 42** (аугментация размытием по Гауссу/defocus — новая идея, смешанный/маргинальный результат по honest 5-сидовой проверке, не отгружено): та же логика «фото экрана/распечатки телефоном», что уже дала устойчивость к повороту (round 19/23), яркости (round 30) и сдвигу (round 40) — реальное фото, снятое рукой, может быть не в фокусе или слегка смазано, а тестовый датасет состоит из прямых цифровых экспортов. Добавлена `gaussian_blur` в `app/ml/augmentation.py` (встроенный фильтр PIL, без новой зависимости).

Проверено на 5 сидах (42, 7, 99, 123, 2024), размытые копии добавлены поверх текущего шипованного рецепта (поворот+отражение+яркость/контраст+сдвиг) — тем же способом «дополнительной параллельной группы», что и round 38-40 (копии базового поворот+отражение+яркость набора, не самого сдвинутого):

| сид | accuracy: прод → +blur | DME precision: прод → +blur | DME recall: прод → +blur |
|---|---|---|---|
| 42 | 0.953 → 0.957 | 0.857 → 0.860 | 0.878 → **0.902** |
| 7 | 0.964 → 0.964 | 0.867 → 0.867 | 0.867 → 0.867 |
| 99 | 0.967 → 0.972 | 0.895 → 0.944 | 0.773 → 0.773 |
| 123 | 0.986 → 0.986 | 0.974 → 0.974 | 0.949 → 0.949 |
| 2024 | 0.960 → 0.960 | 0.903 → 0.931 | 0.824 → **0.794** |
| **среднее** | **0.966 (±0.012) → 0.968 (±0.012)** | **0.899 (±0.046) → 0.915 (±0.050)** | **0.858 (±0.066) → 0.857 (±0.074)** |

Честно смешанная картина, не такая чистая, как round 40: accuracy и DME precision ни разу не просели ни на одном сиде (accuracy выросла на 2 из 5, precision — на 2 из 5), но обе прибавки по модулю меньше или сопоставимы с собственным разбросом дельты между сидами (accuracy +0.18 п.п. при std дельты 0.24 п.п.; precision +1.62 п.п. при std дельты 2.21 п.п.) — то есть направление стабильно неотрицательное, но величина эффекта статистически неотличима от шума. DME recall — метрика, которую продукт ставит на первое место (round 5/6: пропущенный отёк хуже лишней проверки), — честно нейтральна: выросла на сиде 42 (+2.4 п.п.), просела на сиде 2024 (-2.9 п.п.), не изменилась на остальных 3, в среднем практически ноль (-0.1 п.п.). В отличие от round 40 (recall никогда не просаживался больше, чем в пределах шума, при этом чаще рос) и round 30 (recall был плоским, но точность и precision выросли заметно, вне шума — крупнейший прирост precision на тот момент), здесь ни один из двух аргументов «за» не набирает достаточной силы: эффект на accuracy/precision слабее шума, а recall — не плоский-но-безопасный, а буквально ноль в среднем с реальной просадкой на одном сиде, не компенсированной чем-то однозначно более крупным.

Отдельно: отгрузка потребовала бы новой архитектуры в `train.py` — сейчас сдвиг (round 40) является единственной терминальной стадией в конце цепочки; добавление размытия как второй параллельной терминальной аугментации (а не хвостом после сдвига — так тестировалось) означало бы не просто регулировку флагов, а реальные изменения в `_run_select_phase`/`_run_final_refit_phase`/`_multi_seed_worker_evaluate`, ещё не написанные и не проверенные. Не отгружено. Функция `gaussian_blur` осталась в `app/ml/augmentation.py` (протестирована юнит-тестами) на случай, если у будущего раунда появится более сильная гипотеза (например, более широкий диапазон радиуса) или готовность делать двойную терминальную стадию.

**ML round 43** (гиперпараметры SVM C/gamma — повторная проверка после round 40 удвоил выборку ещё раз, снова отрицательно): по тому же прецеденту, что round 33 (перепроверка после утроения выборки round 29/30) и round 41 (перепроверка PCA после того же изменения), — проверил `C` и `gamma` на одном сиде против актуального полностью аугментированного рецепта (поворот+отражение+яркость/контраст+сдвиг):

| C | gamma | cv balanced accuracy |
|---|---|---|
| 0.5 | scale | 0.8941 (±0.0247) |
| **1.0 (текущий прод)** | **scale** | **0.8919 (±0.0219)** |
| 2.0 | scale | 0.8855 (±0.0217) |
| 5.0 | scale | 0.8786 (±0.0221) |
| 1.0 | 0.01 | 0.5105 (±0.0036) |
| 1.0 | 0.005 | 0.7547 (±0.0050) |

Тот же вывод, что и round 33 на предыдущем рецепте: дефолт (`C=1.0, gamma="scale"`) остаётся лучшим или неразличимо близким к лучшему — `C=0.5` формально чуть выше (+0.22 п.п.), но это на порядок меньше стандартного отклонения обоих (±0.022-0.025) — шум, не сигнал. Рост `C` снова монотонно ухудшает результат. Явно заданная маленькая `gamma` снова катастрофична (0.01 — уровень случайного угадывания, почти точно то же число, что round 33 получил на другом рецепте — 0.5105 против 0.5089; 0.005 — тоже сильно хуже дефолта). Результат отрицательный уже на разведочной проверке одним сидом — полная 5-сидовая верификация не проводилась. Изменений в коде и чекпоинте нет.

**ML round 44** (аугментация перспективным искажением/keystone — положительно на всех трёх метриках по honest 5-сидовой проверке, отгружено; заодно архитектура аугментации в `train.py` обобщена с одной терминальной стадии на произвольное число параллельных «сиблинг»-групп): та же логика «фото экрана/распечатки телефоном», что уже дала устойчивость к повороту (round 19/23), яркости (round 30) и сдвигу (round 40) — реальное ручное фото редко снято строго перпендикулярно, что даёт трапециевидное искажение (keystone), которое чистый поворот не моделирует. Добавлена `random_perspective` в `app/ml/augmentation.py`: случайно смещает все 4 угла изображения внутрь (до 6% ширины/высоты независимо по каждой оси), решает систему уравнений на 8 коэффициентов гомографии (PIL этого не делает сам) и применяет `Image.transform(..., Image.PERSPECTIVE, ...)`; освободившаяся область дозаполняется чёрным (та же логика, что и `random_shift` round 40).

Проверено на 5 сидах (42, 7, 99, 123, 2024) той же методологией «дополнительной параллельной группы», что и round 38-42: перспективные копии добавлены поверх текущего шипованного рецепта (поворот+отражение+яркость/контраст+сдвиг), сравнение честное — «шипованное (база+сдвиг)» vs «+перспектива»:

| сид | accuracy: прод → +перспектива | DME precision: прод → +перспектива | DME recall: прод → +перспектива |
|---|---|---|---|
| 42 | 0.953 → 0.957 | 0.857 → 0.844 | 0.878 → **0.927** |
| 7 | 0.964 → 0.973 | 0.867 → 0.900 | 0.867 → 0.900 |
| 99 | 0.967 → 0.981 | 0.895 → 0.950 | 0.773 → **0.864** |
| 123 | 0.986 → 0.982 | 0.974 → 0.973 | 0.949 → 0.923 |
| 2024 | 0.960 → 0.969 | 0.903 → **1.000** | 0.824 → 0.794 |
| **среднее** | **0.966 (±0.016) → 0.973 (±0.014)** | **0.899 (±0.046) → 0.933 (±0.062)** | **0.858 (±0.066) → 0.882 (±0.052)** |

Самый чистый результат за всю серию раундов аугментации: accuracy выросла на 4 из 5 сидов (просела на сиде 123, но всего на 0.46 п.п. — заметно меньше межсидового разброса), DME precision выросла заметно на 3 из 5 (+4.3, +5.5, +9.7 п.п.) и просела незначимо на оставшихся 2 (-1.3, -0.1 п.п.). DME recall — метрика, которую продукт ставит на первое место, — выросла заметно на 3 из 5 (+4.9, +9.1, +3.3 п.п.) и просела умеренно на 2 (-2.6, -2.9 п.п.) — прибавки заметно крупнее просадок как по частоте, так и по величине. Сравнение с прецедентами: сильнее round 40 (там средний прирост recall был меньше, +0.6 п.п.) и сильнее round 30 (тот отгружался при плоском recall, оправданный крупным приростом precision — здесь прирост precision ещё крупнее, а recall дополнительно чистый нетто-плюс). **Отгружено**, включено по умолчанию (`--no-perspective-augment` для отката).

Отгрузка потребовала архитектурного обобщения: до этого раунда единственной параллельной («терминальной») стадией был сдвиг (round 40) — `_apply_terminal_augmentation_stage` удваивала labels/groups внутри себя, поддерживая ровно одну такую стадию. Добавление перспективы как ВТОРОЙ независимой параллельной группы (а не хвостом после сдвига — так тестировалось) этого не позволяло: два последовательных вызова той же функции рассинхронизировали бы длины labels/groups и фактического роста X. Заменена на `_extract_sibling_augmentation_features`, которая возвращает только новую матрицу признаков одной группы; вызывающий код сам конкатенирует X/y/groups от ФИКСИРОВАННОЙ базовой ссылки (пост-поворот+отражение+яркость), захваченной до добавления любых сиблингов — это позволяет добавить произвольное число параллельных групп без повторения бага. Применено во всех трёх местах (`_run_select_phase`, `_run_final_refit_phase`, `_multi_seed_worker_evaluate`); память подтверждена стабильной на реальном продовом переобучении даже с двумя сиблинг-стадиями одновременно (пик ~14.25ГБ RSS на финальном рефите — тот же порядок, что и round 40 с одной стадией, поскольку каждая сиблинг-стадия по-прежнему извлекает признаки по одному изображению за раз, не удваивая список сырых пикселей).

По пути также нашёл и исправил реальную неэффективность в `select_best_model`: все SVM-кандидаты собирались с `probability=True`, что заставляет sklearn делать внутреннюю 5-фолдовую калибровку Платта внутри каждого `.fit()` — но `cross_val_score(..., scoring="balanced_accuracy")` вызывает только `.predict()`, который не зависит от этой калибровки вообще (результат идентичен что с `probability=True`, что без). На реальных данных это обошлось: первая попытка продового переобучения на новом (удвоенном ещё раз, сиблинг+сиблинг) рецепте зависла на кандидате `svm_rbf` (без PCA) больше 7 часов без признаков зависания процесса (память стабильна, CPU 100%) — просто честно тратила впустую ~4-5x времени на неиспользуемую калибровку. Исправлено: `_candidate_estimators` получила параметр `probability` (по умолчанию `True` для обратной совместимости), `select_best_model` теперь оценивает кандидатов с `probability=False` и повторно достаёт победителя с `probability=True` только для реального дообучения (там `predict_proba` уже нужен — пороги DME, калибровка). Замерено на синтетическом бенчмарке: 72.5с → 19.6с, те же cv-оценки до последней цифры. С исправлением повторное продовое переобучение прошло полный select-фазу (все 6 кандидатов) за ~2.5 часа вместо 7+ и не оконченных.

Продовое переобучение (`svm_rbf_pca75`, тот же победитель, что в round 30/31/40) воспроизвело цифры разведочной проверки почти до знака: held-out test accuracy 0.957, честная multi-seed-характеризация — accuracy 0.973 (±0.009), DME precision 0.933 (±0.055), DME recall 0.882 (±0.049) — совпадает с независимым 5-сидовым скриптом выше буквально до последней цифры на каждом сиде. На чувствительном скрининговом пороге (0.089, подобран автоматически): precision 0.81 / recall 0.95. Обучающая выборка для финального чекпоинта — 66780 снимков (1113 → ×5 поворот → ×2 отражение → ×2 яркость = 22260 базовых → +22260 сдвиг-сиблинг → +22260 перспектива-сиблинг). Чекпоинт и модель-карта переобучены и отгружены. Полный набор тестов (66) проходит.

**ML round 45** (повторная проверка размытия по Гауссу из round 42 — теперь поверх нового рецепта и с новой архитектурой, снова отрицательно, не отгружено): round 42 нашёл смешанный/маргинальный результат для `gaussian_blur` поверх рецепта того времени (база+сдвиг) и явно отложил отгрузку из-за архитектурного ограничения — тогда `train.py` поддерживал ровно одну параллельную сиблинг-стадию. Round 44 построил именно нужное обобщение (`_extract_sibling_augmentation_features`, произвольное число параллельных групп), так что честно перепроверил размытие как ТРЕТЬЮ параллельную группу поверх актуального шипованного рецепта (база+сдвиг+перспектива) — раз базовый рецепт изменился (перспектива уже дала реальный прирост recall), стоило знать, помогает ли размытие ещё, вредит или остаётся шумом на новом фоне.

Дешёвая разведочная проверка на одном сиде (42) дала на удивление однозначный ответ: `shipped+blur` (n=70240) выдал ТЕ ЖЕ ЧИСЛА, что и `shipped` (n=52680) без округления — accuracy 0.9574, DME precision 0.8444, DME recall 0.9268 совпадают до 4 знака в обоих случаях. Это не «эффект в пределах шума» (как round 42 нашёл на предыдущем рецепте) — это буквально нулевой эффект на этом сиде: модель приняла те же решения на тесте что с размытыми копиями в обучении, что без них. В сочетании с уже имеющимся полным 5-сидовым прогоном round 42 (на предыдущем рецепте: эффект на accuracy/precision слабее межсидового шума, DME recall в среднем практически ноль) это даёт достаточно уверенности, чтобы не проводить дорогую повторную 5-сидовую верификацию — по тому же прецеденту, что round 43 (отрицательный результат уже на разведочной проверке одним сидом, полная 5-сидовая верификация не проводилась). **Не отгружено**, изменений в коде и чекпоинте нет; `gaussian_blur` остаётся в `app/ml/augmentation.py` (протестирована юнит-тестами) на случай более сильной гипотезы в будущем (например, более узкий диапазон радиуса, ближе к лёгкой расфокусировке, а не заметному размытию).

**ML round 46** (расширение TTA (round 34/35) с 5 видов до 7 добавлением сдвига и перспективы — честно смешанный результат по 5-сидовой проверке, требует решения пользователя, не отгружено): текущий прод усредняет вероятности по 5 видам одного снимка (оригинал + повороты ±5° + отражение + один вариант яркости/контраста, round 34/35), но сдвиг (round 40) и перспектива (round 44) с тех пор вошли в обучающий рецепт и оба дали реальный прирост — ни разу не были добавлены в набор видов TTA на инференсе. Честно проверил, помогает ли расширение TTA до 7 видов (+сдвиг +перспектива) дальше, той же методологией, что round 34/35: 5 сидов (42, 7, 99, 123, 2024), пороги подобраны той же OOF CV-методологией на обучающей выборке (round 6/10), тест-сет оценён один раз на всех трёх операционных точках — дефолтный 50%-порог, best-F1 и чувствительный клинический (high-recall).

На **best-F1 пороге** результат чистый, как round 40:

| сид | precision: TTA-5(прод) → TTA-7 | recall: TTA-5(прод) → TTA-7 |
|---|---|---|
| 42 | 0.864 → 0.884 | 0.927 → 0.927 |
| 7 | 0.929 → 0.931 | 0.867 → **0.900** |
| 99 | 0.947 → 0.950 | 0.818 → **0.864** |
| 123 | 0.974 → 0.974 | 0.949 → 0.949 |
| 2024 | 0.903 → 0.906 | 0.824 → **0.853** |
| **среднее** | **0.923 (±0.038) → 0.929 (±0.032)** | **0.877 (±0.053) → 0.898 (±0.036)** |

Recall не просел ни разу ни на одном из 5 сидов (вырос на 3, не изменился на 2), precision тоже не просела ни разу (выросла на 4, не изменилась на 1) — на этом пороге TTA-7 строго лучше TTA-5.

На **чувствительном клиническом пороге** (`DME_SCREENING_THRESHOLD`, тот, что реально используется в проде для скрининга) картина честно другая:

| сид | precision: TTA-5(прод) → TTA-7 | recall: TTA-5(прод) → TTA-7 |
|---|---|---|
| 42 | 0.755 → 0.702 | 0.976 → 0.976 |
| 7 | 0.931 → **0.818** | 0.900 → 0.900 |
| 99 | 0.947 → 0.950 | 0.818 → 0.864 |
| 123 | 0.925 → **0.844** | 0.949 → 0.974 |
| 2024 | 0.857 → 0.882 | 0.882 → 0.882 |
| **среднее** | **0.883 (±0.071) → 0.839 (±0.082)** | **0.905 (±0.055) → 0.919 (±0.047)** |

Recall снова не просел ни разу (вырос на 2, не изменился на 3) — приоритетная метрика продукта в безопасности. Но precision здесь реально просела на 3 из 5 сидов, на одном (сид 7) — заметно, на 11.3 п.п. На дефолтном 50%-пороге картина смешанная ещё сильнее: recall вырос на 3 сидах, но просел на сиде 99 (-4.5 п.п., вместе с precision -5.6 п.п. на том же сиде) — этот порог, в отличие от двух других, не даёт чистого сигнала.

В отличие от round 35's own TTA-5 (там recall не просел вообще нигде ни на каком из двух проверенных порогов, при более скромной, но однонаправленной просадке precision), здесь есть один сид (99) с настоящей просадкой recall на дефолтном пороге и заметные просадки precision на клиническом пороге (до -11 п.п.) — не такой однозначно чистый результат. Плюс это тот же компромисс задержка/стоимость, что round 35 явно вынес на решение пользователя, а не решил в одностороннем порядке внутри автономного цикла — только здесь стоимость растёт снова (5x → 7x извлечений признаков на запрос), а выгода менее однозначна, чем в прошлый раз.

**Не отгружено автономно.** Изменений в `inference.py` и чекпоинте нет — результат честно задокументирован здесь как открытый вопрос, ждущий решения пользователя (расширять TTA дальше ценой ещё большей задержки/стоимости запроса, с чистым выигрышем на best-F1 пороге, но реальной ценой по precision на клиническом пороге — или оставить как есть).

**ML round 47** (аугментация виньетированием — новая идея, отрицательно на разведочной проверке, не отгружено): та же логика «фото экрана/распечатки телефоном», что уже дала устойчивость к повороту (round 19/23), сдвигу (round 40) и перспективе (round 44) — реальная фотография с телефона нередко темнее по краям/углам относительно центра (падение освещённости объектива + неравномерный внешний свет), в отличие от прямых цифровых экспортов датасета. Добавлена `random_vignette` в `app/ml/augmentation.py`: затемняет каждый пиксель множителем, квадратично убывающим с расстоянием от центра (1.0 в центре, до `1 - strength` в самом дальнем углу) — плавный радиальный спад, а не жёсткая маска. Категория, в которую эта идея попадает (геометрическое/световое искажение), ближе к трём уже отгруженным аугментациям, чем к трём отклонённым (спекл-шум round 38, JPEG-артефакты round 39, размытие round 42/45 — все пиксельно-текстурные, не геометрические/световые).

Дешёвая разведочная проверка на одном сиде (42), той же сиблинг-методологией, что round 44-46 (виньетированные копии добавлены поверх актуального шипованного рецепта база+сдвиг+перспектива), дала тот же результат, что и round 45's Гауссово размытие: `shipped+vignette` (n=70240) выдал ТЕ ЖЕ ЧИСЛА, что и `shipped` (n=52680) без округления — accuracy 0.9574, DME precision 0.8444, DME recall 0.9268 совпадают до 4 знака в обоих случаях. Не «эффект в пределах шума» — буквально нулевой эффект на этом сиде: несмотря на другую геометрическую категорию по сравнению с блюром/шумом/JPEG, конкретно эта гипотеза (виньетирование в измеренном диапазоне силы 0.15–0.45) тоже не изменила ни одного решения модели на тестовой выборке. По тому же прецеденту, что round 43 и 45 (однозначно отрицательный сигнал уже на разведочной проверке одним сидом), полная 5-сидовая верификация не проводилась. **Не отгружено**, изменений в чекпоинте нет; `random_vignette` остаётся в `app/ml/augmentation.py` (протестирована юнит-тестами) на случай более сильной гипотезы в будущем (например, другой диапазон силы, или несимметричное/направленное затемнение вместо радиального).

**ML round 48** (аугментация зумом/масштабом — новая идея, честно отрицательно по полной 5-сидовой проверке, не отгружено): та же логика «фото экрана/распечатки телефоном», но новый геометрический аспект — реальное ручное фото редко снято с одного и того же расстояния каждый раз, так что снятое содержимое может казаться крупнее или мельче в кадре. В отличие от трёх уже отгруженных геометрических искажений (поворот меняет угол, сдвиг переносит, перспектива скашивает), ни одно из них не меняет масштаб. Добавлена `random_zoom` в `app/ml/augmentation.py`: приближение обрезает центр увеличенной копии обратно до исходного размера; отдаление вставляет уменьшенную копию по центру чёрного холста — та же конвенция «потерянное/освободившееся содержимое — чёрное, не растянутое и не обёрнутое», что и у `random_shift`/`random_perspective`.

Разведочная проверка на одном сиде (42) впервые за несколько раундов дала не «нулевой эффект» (как round 45/47), а настоящий сдвиг метрик — но в отрицательную сторону: DME recall просел на 7.3 п.п. (0.927→0.854). Это не так однозначно, как размытие/виньетирование (там числа совпадали до знака), и recall у этого проекта исторически колеблется на ±5-7 п.п. между сидами — поэтому, в отличие от round 43/45/47, разведочного результата было недостаточно для вывода: понадобилась полная честная 5-сидовая верификация, той же методологией, что round 38-44 (зум-копии поверх актуального шипованного рецепта база+сдвиг+перспектива):

| сид | accuracy: прод → +zoom | DME precision: прод → +zoom | DME recall: прод → +zoom |
|---|---|---|---|
| 42 | 0.957 → 0.949 | 0.844 → 0.854 | 0.927 → **0.854** |
| 7 | 0.973 → 0.960 | 0.900 → 0.889 | 0.900 → **0.800** |
| 99 | 0.981 → 0.977 | 0.950 → 0.947 | 0.864 → **0.818** |
| 123 | 0.982 → 0.982 | 0.973 → 1.000 | 0.923 → 0.897 |
| 2024 | 0.969 → 0.973 | 1.000 → 0.967 | 0.794 → **0.853** |
| **среднее** | **0.973 (±0.009) → 0.968 (±0.012)** | **0.933 (±0.055) → 0.931 (±0.053)** | **0.882 (±0.049) → 0.844 (±0.034)** |

Честный и, в отличие от round 42's действительно неоднозначного результата, лопнутый в одну сторону: DME recall просел на 4 из 5 сидов (на трёх — заметно, от -2.6 до -10 п.п.), вырос лишь на одном (+5.9 п.п., меньше, чем крупнейшая просадка) — направление стабильно отрицательное, а не случайный шум вокруг нуля. Accuracy и precision не дают компенсирующего плюса: обе в среднем слегка отрицательны или нейтральны (accuracy -0.44 п.п., precision -0.22 п.п.), ни одна не растёт настолько, чтобы оправдать просадку главной продуктовой метрики. Первый честный «настоящий минус», а не «нулевой эффект» или «неразличимо от шума», за всю серию раундов 42-48. **Не отгружено**, изменений в чекпоинте нет; `random_zoom` остаётся в `app/ml/augmentation.py` (протестирована юнит-тестами) — возможно, более узкий диапазон масштаба (например, ±5% вместо ±15%) поведёт себя иначе, но это отдельная, ещё не проверенная гипотеза.

**ML round 49** (число компонент PCA — повторная проверка после round 44 добавил перспективу, число не изменилось): по тому же прецеденту, что round 31 (после flip) и round 41 (после shift), — round 44 добавил перспективу как вторую сиблинг-группу, снова изменив состав обучающей выборки (52680 вместо 35120 признаковых векторов, хотя количество базовых изображений то же). Дешёвая проверка на одном сиде — CV balanced accuracy на актуальном полностью аугментированном рецепте (поворот+отражение+яркость/контраст+сдвиг+перспектива), без переобучения финального чекпоинта:

| n_components | cv balanced accuracy |
|---|---|
| 50 | 0.8872 (±0.0219) |
| **75 (текущий прод)** | **0.8913 (±0.0197)** |
| 100 | 0.8940 (±0.0208) |
| 150 | 0.8901 (±0.0228) |
| 200 | 0.8887 (±0.0231) |

Тот же вывод, что и round 31/41: разброс между всеми пятью значениями — 0.68 п.п. (0.8872–0.8940), заметно меньше собственного стандартного отклонения любого из них (±1.97–2.31 п.п.) — неразличимо в пределах шума. `n_components=100` формально чуть выше текущего прода (+0.27 п.п.), но это на порядок меньше шума. Текущий прод (75) остаётся разумным выбором. Полная 5-сидовая проверка не проводилась — экспериментальный сигнал уже в пределах шума. Изменений в коде и чекпоинте нет.

**ML round 50** (аугментация зумом — повторная проверка с более узким диапазоном ±5% вместо round 48's ±15%, честно отрицательно по полной 5-сидовой проверке, не отгружено): round 48 явно оставил открытой гипотезу, что более узкий диапазон масштаба поведёт себя иначе — там recall просел на 4 из 5 сидов при ±15%. Проверено на том же `random_zoom` из `app/ml/augmentation.py` с `scale_range=(0.95, 1.05)`.

Разведочная проверка на одном сиде (42) дала не «нулевой эффект» (как блюр/виньетирование), а настоящий, но на этот раз позитивный сдвиг: accuracy 0.9574→0.9617, DME precision 0.8444→0.8636, DME recall не изменился (0.9268→0.9268). Раз это не буквально идентичные числа по всем трём метрикам, по тому же прецеденту, что round 48 (реальный, не нулевой эффект требует полной верификации независимо от направления), результата одного сида было недостаточно для вывода — понадобилась полная честная 5-сидовая верификация той же методологией:

| сид | accuracy: прод → +zoom(±5%) | DME precision: прод → +zoom(±5%) | DME recall: прод → +zoom(±5%) |
|---|---|---|---|
| 42 | 0.957 → 0.962 | 0.844 → 0.864 | 0.927 → 0.927 |
| 7 | 0.973 → 0.969 | 0.900 → 0.926 | 0.900 → **0.833** |
| 99 | 0.981 → 0.972 | 0.950 → 0.900 | 0.864 → **0.818** |
| 123 | 0.982 → 0.986 | 0.973 → 1.000 | 0.923 → 0.923 |
| 2024 | 0.969 → 0.969 | 1.000 → 1.000 | 0.794 → 0.794 |
| **среднее** | **0.973 (±0.009) → 0.972 (±0.008)** | **0.933 (±0.055) → 0.938 (±0.052)** | **0.882 (±0.055) → 0.859 (±0.055)** |

Более узкий диапазон (±5%) действительно смягчил проблему round 48: на трёх сидах из пяти (42, 123, 2024) — буквально нулевой эффект, DME recall совпадает до знака; на оставшихся двух (7, 99) recall всё равно проседает (-6.7 и -4.6 п.п. соответственно), причём никогда не растёт ни на одном сиде. Accuracy в среднем не меняется (-0.1 п.п., шум), precision в среднем чуть растёт (+0.4 п.п.), но этот плюс распределён неравномерно и не компенсирует просадку recall там, где она случается — а recall для этой задачи важнее accuracy/precision. Направление сигнала («recall никогда не растёт, иногда заметно падает») то же самое, что и у round 48 при ±15%, просто слабее по величине и реже случается. **Не отгружено**, изменений в чекпоинте нет; `random_zoom` остаётся в `app/ml/augmentation.py` без изменений — обе проверенные гипотезы диапазона масштаба (±15% в round 48, ±5% здесь) отклонены, аугментация зумом закрыта как направление, пока не появится качественно другая идея (например, зум только в одну сторону, а не в обе).

**ML round 51** (аугментация бликом/specular highlight — новая категория, однозначно отрицательно уже на разведочной проверке, не отгружено): в отличие от всех предыдущих идей этой серии — не пиксельно-текстурная (спекл-шум round 38, JPEG round 39, размытие round 42/45 — все отклонены), не равномерное геометрическое/радиальное искажение (виньетирование round 47, зум round 48/50 — оба отклонены). Реальный источник: глянцевый экран телефона или монитора нередко отражает окно, лампу или вспышку камеры обратно в объектив, давая яркое локальное пятно где-то в кадре — в отличие от виньетирования (равномерное затемнение от центра к краям), блик строго аддитивен (свет только добавляется, никогда не вычитается) и расположен случайно, а не всегда по центру. Добавлена `random_glare` в `app/ml/augmentation.py`: центр блика — случайная точка по всему холсту (в том числе частично за кадром), яркость вокруг него спадает по Гауссиане.

Разведочная проверка на одном сиде (42) дала не «нулевой эффект» и не пограничный сдвиг, а однозначный, крупный минус сразу по всем трём метрикам: accuracy 0.9574→0.9319 (-2.55 п.п.), DME precision 0.8444→0.7907 (-5.37 п.п.), DME recall 0.9268→0.8293 (-9.75 п.п.). В отличие от round 48's зума (где отрицательный сдвиг был только в recall и сопоставим по величине с естественным межсидовым шумом recall, ±5-7 п.п.) — здесь просела ещё и accuracy, у которой межсидовый шум исторически намного меньше (±0.9-2.2 п.п. по прошлым раундам), а просадка (-2.55 п.п.) её кратно превышает. Это тот же уровень однозначности, что и round 43/45/47 (уверенный отрицательный сигнал уже на разведочном сиде) — только у тех эффект был либо нулевым, либо влиял на одну метрику, а здесь просели сразу все три и заметно. По тому же прецеденту (round 43: явно отрицательный сигнал на разведочной проверке не требует дорогой 5-сидовой верификации), полная проверка не проводилась. **Не отгружено**, изменений в чекпоинте нет; `random_glare` остаётся в `app/ml/augmentation.py` (протестирована юнит-тестами) на случай другой гипотезы в будущем (например, заметно меньшая сила/радиус блика).

**ML round 52** (SVM `C`/`gamma` — повторная проверка после round 44 добавил перспективу, ничего не изменилось): по тому же прецеденту, что round 33/43 (сам `C`/`gamma`) и round 31/41/49 (PCA `n_components`) — round 43 перепроверял `C`/`gamma` сразу после того, как round 40 добавил сдвиг (4-я стадия, 1-я сиблинг-группа), но ни разу после того, как round 44 добавил перспективу (5-я стадия, 2-я сиблинг-группа), снова изменив состав обучающей выборки. Group-aware CV (`StratifiedGroupKFold`, тот же `cv_folds=4`/`random_state=42`, что и в `select_best_model`) на одном сиде (42) на актуальном полностью аугментированном рецепте:

| C (gamma=`scale`, текущий прод) | cv balanced accuracy |
|---|---|
| 0.25 | 0.8971 (±0.0243) |
| 0.5 | 0.8956 (±0.0226) |
| **1.0 (текущий прод)** | **0.8913 (±0.0197)** |
| 2.0 | 0.8864 (±0.0189) |
| 4.0 | 0.8802 (±0.0200) |

| gamma (C=1.0, текущий прод) | cv balanced accuracy |
|---|---|
| **scale (текущий прод)** | **0.8913 (±0.0197)** |
| 0.001 | 0.8904 (±0.0192) |
| 0.005 | 0.7698 (±0.0041) |
| 0.01 | 0.5163 (±0.0045) |
| 0.05 | 0.5000 (±0.0000) |

Тот же вывод, что и round 43: `C=0.25` формально чуть выше текущего прода (+0.58 п.п.), но это меньше собственного стандартного отклонения (±2.43 п.п.) — шум; рост `C` монотонно ухудшает результат, тот же паттерн, что и раньше. Явно заданная маленькая `gamma` снова катастрофична — и на этот раз даже более резко, чем в round 33/43: `gamma=0.05` полностью коллапсирует в вырожденный классификатор, предсказывающий один и тот же класс всегда (0.5000 ровно, ±0.0000 — нулевой разброс между фолдами). Дефолт (`C=1.0, gamma=scale`) остаётся лучшим или неразличимо близким к лучшему. Полная 5-сидовая проверка не проводилась — сигнал уже отрицательный/шумовой, тот же прецедент, что round 41/43/49. Изменений в коде и чекпоинте нет.

**ML round 53** (число фолдов CV в `select_best_model` — впервые перепроверено, ничего не изменилось): в отличие от `C`/`gamma` (round 33/43/52) и `n_components` PCA (round 31/41/49), сам `cv_folds=4` ни разу не подвергался пересмотру за всю сессию, хотя обучающая выборка менялась многократно. На одном сиде (42) на актуальном рецепте (657 пациентов в трейне, 84 с DME — достаточно даже для 8 фолдов):

| cv_folds | cv balanced accuracy |
|---|---|
| 3 | 0.9023 (±0.0094) |
| **4 (текущий прод)** | **0.8913 (±0.0197)** |
| 5 | 0.8960 (±0.0284) |
| 6 | 0.8915 (±0.0283) |
| 8 | 0.8953 (±0.0462) |

Разброс между всеми пятью значениями — 1.10 п.п. (0.8913–0.9023), меньше или сопоставимо с собственным стандартным отклонением большинства из них (±0.94–4.62 п.п., причём разброс растёт вместе с числом фолдов — ожидаемо: чем больше фолдов, тем меньше и шумнее каждый тестовый кусок) — тот же паттерн шума, что и во всех прошлых пересмотрах. Текущий прод (4) остаётся разумным выбором: не худший и не лучший по номиналу, но всё в пределах неразличимости. Полная 5-сидовая проверка не проводилась. Изменений в коде и чекпоинте нет.

**ML round 54** (параметр `orientations` внутри самого HOG — впервые перепроверен, ничего не изменилось; по пути пойман и исправлен реальный OOM в разведочном скрипте): в отличие от `C`/`gamma`, PCA `n_components` и `cv_folds` (round 33-53), которые влияют только на этап классификатора после уже готовых признаков, `orientations` (сейчас 8, как и `pixels_per_cell`/`cells_per_block`/`HOG_SIZE` — все ни разу не пересматривались) меняет само представление признаков, поэтому требует переизвлечения, а не только переобучения. Ради разумной стоимости проверено на базовом рецепте (поворот+отражение+яркость, 17560 сэмплов) вместо полного (52680) — вопрос про `orientations` не зависит от того, какие геометрические сиблинг-стадии добавлены поверх.

Первая версия разведочного скрипта заранее конвертировала все 17560 базовых изображений в float32-градации серого одним списком, чтобы переиспользовать между всеми 5 проверяемыми значениями `orientations` — и была честно убита OOM (`dmesg` подтвердил: `Killed process ... anon-rss:15989048kB`, потолок машины). Ровно та же ошибка, что `_extract_sibling_augmentation_features` уже решала в реальном пайплайне (round 38/40) — одновременное хранение двух полных списков сырых изображений, только здесь усугублённая переходом uint8→float32 (4-кратный рост памяти на тот же список, который остальные раунды сессии спокойно держат как uint8). Исправлено: признаки для каждого значения `orientations` теперь считаются по одному изображению за раз прямо из uint8-исходника, без промежуточного общего списка — тот же безопасный паттерн, что и везде в `train.py`.

После исправления сид 42 на актуальной базе:

| orientations | размерность признаков | cv balanced accuracy |
|---|---|---|
| 4 | 794 | 0.8802 (±0.0301) |
| 6 | 1186 | 0.8905 (±0.0313) |
| **8 (текущий прод)** | **1578** | **0.8901 (±0.0250)** |
| 10 | 1970 | 0.8888 (±0.0276) |
| 12 | 2362 | 0.8885 (±0.0301) |

Разброс между всеми пятью значениями — 1.03 п.п. (0.8802–0.8905), меньше собственного стандартного отклонения каждого из них (±2.50–3.13 п.п.) — тот же шумовой паттерн, что и во всех прошлых пересмотрах. Текущий прод (8) практически неотличим от номинально лучшего значения (6, +0.04 п.п.). Полная 5-сидовая проверка не проводилась — сигнал уже в пределах шума на одном сиде. Изменений в коде и чекпоинте нет.

**Round 55** (по прямому запросу пользователя: `app/services/segmentation.py` — две новые категории патологий, `subretinal_hyperreflective_material` и `drusen`; по пути найден и исправлен реальный краевой артефакт в новом коде; проверено время обработки запроса): round 37 отложил все категории из референсного клинического отчёта кроме двух (интраретинальные кисты/жидкость, субретинальная жидкость), явно объяснив, что оставшиеся требуют другого подхода, чем простой порог по темноте пикселей. Пользователь прислал референсное изображение с 4 категориями разметки и попросил закрыть разрыв. Из оставшихся двух добавленных сейчас — субретинальный гиперрефлективный материал и друзы — обе реализованы тем же принципом «эвристика на уже отслеженной структуре, не обученный классификатор», что и первые две; эпиретинальная мембрана и отслойка пигментного эпителия **сознательно не добавлены** и в этот раз — см. обновлённый докстринг модуля, обе требуют либо надёжного отличия от нормального сигнала ILM (для мембраны), либо опорной линии мембраны Бруха, которую эта модель вообще не отслеживает (для отслойки RPE); честно не заявлять находку — лучше, чем поспешная низкоуверенная эвристика под конкретный клинический термин.

- **Субретинальный гиперрефлективный материал**: зеркальное отражение уже существующего детектора тёмных зон (`dark_mask_in_band`) — та же анатомическая область, что и субретинальная жидкость, но по яркости выше локальной базовой линии, а не ниже. `signal_utils.py` рефакторен: общая логика вычисления базовой линии по строкам вынесена в `_row_baseline`, чтобы `dark_mask_in_band` и новый `bright_mask_in_band` не дублировали её по отдельности.
- **Друзы**: другой принцип обнаружения — не яркость пикселей, а форма самой уже отслеженной границы RPE-хориоидея (`boundaries[_RPE_BOTTOM_IDX]`): локальное выпячивание наружу относительно сильно сглаженной версии этой же кривой, тот же приём, что `_detect_fovea_column` уже использует для самой глубокой точки границы ILM, здесь обобщённый на любое число отдельных выпячиваний по всей ширине снимка.

**Реальный краевой баг, пойманный в собственном новом коде до отгрузки**: сглаживание (`smooth`) неявно дополняет сигнал нулями за границами массива — на прямой (без выпячиваний) синтетической границе это утягивало сглаженную базовую линию вниз у обоих краёв изображения, из-за чего настоящая граница выглядела «выпяченной» относительно искусственно заниженной базовой линии — на ширине примерно в половину окна сглаживания у каждого края. Поскольку у любого реального снимка граница RPE доходит до обоих краёв кадра, без исправления это ложно находило бы «друзы» почти на каждом снимке. Тот же класс краевого артефакта, который `_detect_fovea_column` уже обходит, ограничивая поиск центральными 60% ширины снимка — здесь исправлено аналогично, но отступ привязан к самому окну сглаживания (реальному источнику артефакта), а не к фиксированной доле ширины. Пойман до отгрузки специально написанным регрессионным тестом (`test_detect_drusen_zones_ignores_flat_boundary_edge_smoothing_artifact`) на полностью плоской синтетической границе — без реального изображения баг было бы легко не заметить, т.к. он проявляется только на самом краю кадра.

**Время обработки запроса** (отдельный вопрос пользователя): замерено напрямую через Playwright на реальном сервере (dev, не прод-железо) — от клика «Запустить анализ» до отображения диагноза на странице исследования: **1.2–1.7 сек** сквозно (загрузка файла + анализ + навигация + подгрузка результата), из них сам ML-конвейер (`run_analysis_pipeline`) — **~1.0–1.3 сек** (разметка слоёв/патологий ~0.7 сек, диагноз с 5-видовым TTA ~0.3 сек). Индикатор загрузки уже существовал (спиннер на кнопке «Анализ…» + отдельный спиннер на странице исследования) и корректно покрывает всё время ожидания — с большим запасом укладывается в требование «меньше 5 секунд», доработок не потребовалось.

Найден и исправлен отдельный, не связанный с этим раундом баг: карточка «Карта патологий» на странице исследования хранила свой собственный хардкод того же самого предупреждения о неподдержанных категориях (`app/services/report_generator.py` генерирует то же предупреждение динамически) — после добавления двух новых категорий бэкендный текст обновился автоматически, а фронтендный хардкод остался старым (упоминал уже реализованные друзы и гиперрефлективный материал как «в разработке»). Исправлено вручную в обоих местах.

**Round 56** (проверка калибровки round 55's новых детекторов на реальных снимках): у эвристических зон-детекторов нет попиксельной разметки для валидации (только целостная метка DME/NORMAL на весь снимок) — константы порогов (`BRIGHTNESS_OFFSET`, `DRUSEN_BULGE_OFFSET_FRAC` и т.д.) были выбраны по симметрии с уже существующими, а не откалиброваны на реальных данных. Прогнал `segment_layers` на случайной выборке 200 снимков (сид 42) из реального датасета и посчитал долю снимков, где каждая категория срабатывает:

| категория | сработала на | доля | ср. число зон при срабатывании |
|---|---|---|---|
| Интраретинальные кисты/жидкость (round 37) | 189/200 | 94.5% | 10.23 |
| Субретинальная жидкость (round 37) | 194/200 | 97.0% | 5.01 |
| Субретинальный гиперрефлективный материал (round 55) | 197/200 | 98.5% | 6.46 |
| Друзы (round 55) | 152/200 | 76.0% | 1.61 |

Честный вывод: новые категории срабатывают часто (76-98.5%), но это **не новая проблема, которую я внёс** — уже отгруженные с round 37 категории показывают тот же характер (94.5% и 97.0%), несмотря на многолетнее использование в проде без изменений. Это ожидаемо и согласуется с тем, что этот модуль сам явно документирует с самого начала: «generic darkness detector, not a trained classifier that tells cyst apart from fluid apart from a shadow artifact» — грубый эвристический детектор кандидатных зон для проверки врачом, не точный диагноз. Друзы срабатывают заметно реже остальных (76% против 94.5-98.5%) и с меньшим средним числом зон (1.61 против 5-10) — более консервативный детектор, чем три уже принятых, а не более шумный. Без попиксельной разметки нельзя доказать, что конкретный порог «правильный» — менять константы без чёткого критерия было бы подгонкой под интуицию, а не проверенным улучшением, поэтому пороги оставлены как есть. Изменений в коде нет — только верификация.

**Round 57** (код-ревью роутов `app/api/routes/`; найдена и исправлена реальная уязвимость: неограниченная загрузка файлов): в рамках регулярного код-ревью, не затронутого этим окном раньше, прочитан весь слой API-роутов (`deps.py`, `auth.py`, `studies.py`, `analysis.py`, `patients.py`). `auth.py`/`analysis.py`/`deps.py` — без замечаний (владение исследованием проверяется everywhere, гонки двойного commit уже закрыты предыдущими раундами). В `studies.py` найдена реальная, подтверждённая уязвимость класса OWASP Unrestricted File Upload: эндпоинт `POST /studies` принимал решение о допустимости файла по клиентскому заголовку `Content-Type` (легко подделываемому) и брал расширение для хранения на диске прямо из имени файла, присланного клиентом (`Path(file.filename).suffix`) — реальные байты файла никогда не декодировались и не проверялись как изображение до самого момента ML-анализа (гораздо позже).

**Подтверждено эксплойтом до фикса**: загрузка произвольных не-изображение байт под именем `shell.php` с заголовком `Content-Type: image/jpeg` была принята (201) и сохранена на диск как `<uuid>.php`. Исправлено: реальные байты декодируются и верифицируются через `PIL.Image` (`img.verify()`), расширение для хранения берётся из **проверенного** формата (`JPEG`→`.jpg`, `PNG`→`.png`, `TIFF`→`.tiff`), а не из клиентских метаданных; несовпадение формата или повреждённый файл — явный `415`. Добавлены 2 регрессионных теста (`test_upload_rejects_non_image_bytes_with_spoofed_content_type`, `test_upload_derives_extension_from_actual_content_not_filename`), оба честно проверены на падение против старого кода перед фиксом (`git stash` кода, прогон тестов, `git stash pop`) — первый падал именно так, как и предсказывалось (201 вместо 415). Легитимные загрузки не затронуты: 81/81 backend-тестов проходят.

**Round 58** (продолжение код-ревью; ещё одна реальная уязвимость найдена и исправлена: отсутствие минимальной длины пароля на сервере): просмотрен оставшийся `core/` (`security.py`, `rate_limit.py`, `config.py`), `db/session.py`, `models/*.py`, `schemas/*.py`. `security.py` (bcrypt, JWT с явным allowlist алгоритмов, проверка истечения) и `config.py` (уже есть `assert_production_config_is_safe`, отдельно протестирован) — без замечаний. `rate_limit.py` использует `request.client.host` (реальный TCP-адрес, не заголовок `X-Forwarded-For`) — правильный, неподделываемый выбор; собственная документация файла уже честно называет его ограничения (in-memory, один процесс) — не проблема, которую стоит чинить без запроса.

В `schemas/user.py` найдена реальная уязвимость: `UserCreate.password: str` не имело **никакого** серверного ограничения длины — форма регистрации во фронтенде задаёт `minLength={8}`, но это чисто клиентская проверка, тривиально обходится прямым вызовом API. **Подтверждено до фикса**: регистрация с паролем `"a"` (1 символ) была принята (201), создан настоящий аккаунт с рабочим паролем в один символ. Исправлено: `password: str = Field(min_length=8)` — тот же порог, что уже был заявлен (но не enforced) во фронтенде, теперь единый источник истины на сервере. Добавлены 2 регрессионных теста, падение первого против старого кода подтверждено тем же способом (`git stash`) — 201 вместо ожидаемого 422. 83/83 backend-тестов проходят.

**ML round 59** (параметр `HOG_SIZE` — размер, до которого снимок уменьшается перед извлечением HOG-признаков, сейчас 64x64; последний нетронутый параметр самого HOG после `orientations` в round 54): разведочный CV-свип на базовом рецепте (поворот+отражение+яркость, 17560 сэмплов) по значениям {32, 48, 64, 96, 128} дал явно нешумовой, монотонный сигнал — точность росла, а собственное стандартное отклонение падало на каждом шаге:

| HOG_SIZE | размерность признаков | cv balanced accuracy |
|---|---|---|
| 32 | 298 | 0.8246 (±0.0271) |
| 48 | 810 | 0.8548 (±0.0289) |
| **64 (текущий прод)** | **1578** | **0.8901 (±0.0250)** |
| 96 | 3882 | 0.9107 (±0.0145) |
| 128 | 7210 | 0.9160 (±0.0090) |

В отличие от каждого прошлого пересмотра параметра в этой сессии (все landных в шум), это выглядело как реальный эффект — разброс между значениями многократно превышал собственный std каждого из них. Прежде чем поверить разведочному свипу на слово, честная 5-сидовая multi-seed-проверка на полном продовом рецепте (поворот+отражение+яркость+сдвиг+перспектива) с реальным held-out тестом, тем же протоколом, что и перед каждой отгрузкой в этом проекте, была прогнана для HOG_SIZE=96 (компромисс между 96 и 128 — почти весь прирост CV-точки уже у 96 при вдвое меньшей размерности) против свежепереобученного `svm_rbf_pca75`:

| сид | accuracy (96) | vs прод (64) | DME precision (96) | vs прод | DME recall (96) | vs прод |
|---|---|---|---|---|---|---|
| 42 | 0.9574 | = | 0.8298 | ↓ | 0.9512 | ↑ |
| 7 | 0.9686 | ↓ | 0.8966 | ↓ | 0.8667 | ↓ |
| 99 | 0.9721 | ↓ | 0.8077 | ↓ | 0.9545 | ↑ |
| 123 | 0.9724 | ↓ | 0.9459 | ↓ | 0.8974 | ↓ |
| 2024 | 0.9779 | ↑ | 0.9677 | ↓ | 0.8824 | ↑ |
| **среднее** | **0.9697 (±0.0068)** | | **0.8895 (±0.0626)** | | **0.9104 (±0.0360)** | |
| прод (64), для сравнения | 0.9725 (±0.0089) | | 0.9335 (±0.0553) | | 0.8815 (±0.0492) | |

Честный вывод: **разведочный CV-свип ввёл в заблуждение**. На реальном held-out тесте accuracy не улучшилась (down в 3/5 сидов, flat в 1, up в 1), а DME precision упала **в 5 из 5 сидов** (в среднем −4.4 п.п.) — устойчивый, не шумовой регресс, который CV-свип на тренировочном сплите целиком пропустил (вероятно потому, что CV на аугментированном рецепте с коррелированными сиблинг-копиями завышает внутрисплитовую точность способом, не отражающим генерализацию на реально независимый held-out тест). DME recall смешанный (up в 3/5) — не компенсирует потерю precision. **Не отгружено** — `HOG_SIZE` оставлен равным 64, изменение отменено в коде. Это первый раз в этой сессии, когда полная честная проверка перед отгрузкой развернула многообещающий разведочный результат — прямая иллюстрация того, зачем этот процесс существует. Полный прогон `select_best_model` (все 6 кандидатов) при HOG_SIZE=96 оказался практически непрактичным по времени: non-PCA кандидат `svm_rbf` на 3882-мерных признаках зависал на одном фолде свыше 3.5 часов без сходимости (kernel SVM плохо масштабируется и по числу сэмплов, и по размерности) — эта ветвь ни разу не выигрывала отбор за всю историю проекта, поэтому вместо полного `select_best_model` честная проверка была нацелена сразу на уже известного победителя (`svm_rbf_pca75`) через тот же `_multi_seed_worker_evaluate`, что использует `_multi_seed_evaluation` внутри. Изменений в чекпоинте нет — 83/83 backend-тестов проходят на прежнем HOG_SIZE=64.

**ML round 60** (отрицательный/незначимый, изменений нет): перепроверил последние два нетронутых параметра самого HOG — `pixels_per_cell` (сейчас (8,8)) и `cells_per_block` (сейчас (2,2)), на том же базовом рецепте (17560 сэмплов), сид 42. В отличие от `HOG_SIZE` в round 59, здесь оба параметра дали чистый шумовой паттерн:

| pixels_per_cell (cells_per_block=(2,2)) | размерность | cv balanced accuracy |
|---|---|---|
| (4,4) | 7210 | 0.8858 (±0.0164) |
| (6,6) | 2602 | 0.8871 (±0.0238) |
| **(8,8) (текущий прод)** | **1578** | **0.8901 (±0.0250)** |
| (12,12) | 522 | 0.8836 (±0.0163) |
| (16,16) | 298 | 0.8772 (±0.0362) |

| cells_per_block (pixels_per_cell=(8,8)) | размерность | cv balanced accuracy |
|---|---|---|
| (1,1) | 522 | 0.8888 (±0.0233) |
| **(2,2) (текущий прод)** | **1578** | **0.8901 (±0.0250)** |
| (3,3) | 2602 | 0.8912 (±0.0201) |
| (4,4) | 3210 | 0.8883 (±0.0113) |

Разброс между значениями `pixels_per_cell` — 1.29 п.п. (0.8772–0.8901), меньше собственного стандартного отклонения каждого из них (±1.63–3.62 п.п.); разброс между значениями `cells_per_block` — ещё меньше, 0.29 п.п. (0.8883–0.8912), также в пределах шума (±1.13–2.50 п.п.). В отличие от `HOG_SIZE` (round 59), здесь нет монотонного тренда и разброс многократно меньше шума каждой точки — честный сигнал «без изменений», полная 5-сидовая проверка не потребовалась (по прецеденту, что escalation оправдана только при явно нешумовом сигнале, как это было с `HOG_SIZE`). Текущий прод ((8,8)/(2,2)) остаётся разумным выбором — не худший и не лучший по номиналу, но неразличим в пределах шума. Все параметры самого HOG (`orientations`, `HOG_SIZE`, `pixels_per_cell`, `cells_per_block`) теперь пересмотрены хотя бы раз. Изменений в коде и чекпоинте нет.

**Round 61** (код-ревью `app/services/` и API-достижимой части `app/ml/`; продолжение round 57/58, которые покрыли `api/routes/` и `core/`/`db/`/`models/`/`schemas/`, но не этот слой): прочитаны `pipeline.py`, `report_generator.py`, `diagnosis.py`, `quality_check.py`, `app/ml/inference.py`/`model.py` на предмет path traversal, инъекций и небезопасной десериализации. **Без замечаний**: каждый путь к файлу, который читается или отдаётся клиенту, восходит к серверному `study.image_path` (генерируется как uuid при загрузке, round 57's фикс) — никогда не берётся из клиентского запроса напрямую, и доступ к нему везде проверяется через `_get_own_study` (владение исследованием). `report_generator.py` подставляет в текст отчёта только серверные числовые/категориальные значения (толщина слоёв, вероятности, фиксированные русские метки) — нет пользовательского свободного текста, который попадал бы в отчёт или куда-либо ещё. `joblib.load` и путь к шрифту (`_FONT_PATH`) — всегда фиксированные внутренние константы, не производные от запроса. Вызовы `subprocess.run` в `train.py` — списком аргументов (не `shell=True`), и достижимы только из офлайн CLI-обучения, никогда из веб-API. Изменений в коде нет — только верификация.

**ML round 62** (отрицательный/незначимый, изменений нет): перепроверил собственный диапазон углов поворота (сейчас (-10,-5,5,10)) — единственную аугментационную стадию, чей magnitude-параметр не пересматривался ни разу с момента введения (round 19/23), в отличие от `random_zoom` (round 48/50 пересмотрел свой `scale_range`). Поворот — первая стадия рецепта, от которой строятся все остальные (отражение, яркость, сдвиг, перспектива), поэтому проверено на базовом рецепте (поворот-вариант + отражение + яркость), сид 42:

| набор углов | n (после поворота+отражения+яркости) | cv balanced accuracy |
|---|---|---|
| (-5, 5) (уже) | 10536 | 0.8823 (±0.0289) |
| **(-10, -5, 5, 10) (текущий прод)** | **17560** | **0.8901 (±0.0250)** |
| (-15, -10, -5, 5, 10, 15) (шире, больше углов) | 24584 | 0.8944 (±0.0247) |
| (-15, 15) (шире, то же число углов) | 10536 | 0.8881 (±0.0400) |

Разброс между всеми четырьмя конфигурациями — 1.21 п.п. (0.8823–0.8944), меньше собственного стандартного отклонения каждой из них (±2.47–4.00 п.п.), и без монотонного тренда (у́же и шире диапазоны дают числа вперемешку, не разделяются направленно) — тот же шумовой паттерн, что и HOG `pixels_per_cell`/`cells_per_block` в round 60, а не как `HOG_SIZE` в round 59. Полная 5-сидовая проверка не потребовалась. Текущий прод ((-10,-5,5,10)) остаётся разумным выбором. По пути: конфигурация с 6 углами (24584 сэмплов, самая большая в этом свипе) подняла память процесса до ~14.8ГБ из 15ГБ доступных (без OOM — устояла, но с очень малым запасом) — сам свип не переиспользует изоляцию отдельными подпроцессами, которую использует реальный `train.py` пайплайн; при более широком свипе в будущем стоит взять этот приём. Изменений в коде и чекпоинте нет.

**ML round 63** (отрицательный/незначимый, изменений нет): перепроверил собственные magnitude-параметры двух оставшихся сиблинг-стадий — `random_shift`'s `max_frac` (сейчас 0.08, round 40) и `random_perspective`'s `max_warp_frac` (сейчас 0.06, round 44) — по тому же прецеденту, что zoom (round 50) и поворот (round 62). Каждая проверена как единственная добавленная сиблинг-группа поверх базового рецепта (поворот+отражение+яркость), чтобы изолировать эффект именно этого параметра, извлекая признаки по одному изображению за раз (`_extract_sibling_augmentation_features`) — без риска почти-OOM, который был в round 62 (там варьировалась сама первая стадия рецепта, отсюда рост базового списка изображений):

| shift max_frac | n | cv balanced accuracy | perspective max_warp_frac | n | cv balanced accuracy |
|---|---|---|---|---|---|
| 0.04 | 35120 | 0.8906 (±0.0226) | 0.03 | 35120 | 0.8859 (±0.0244) |
| **0.08 (текущий прод)** | **35120** | **0.8919 (±0.0219)** | **0.06 (текущий прод)** | **35120** | **0.8865 (±0.0262)** |
| 0.12 | 35120 | 0.8951 (±0.0195) | 0.09 | 35120 | 0.8896 (±0.0239) |
| 0.16 | 35120 | 0.8909 (±0.0206) | 0.12 | 35120 | 0.8912 (±0.0228) |

Разброс `shift`: 0.45 п.п. (0.8906–0.8951), без монотонного тренда — шум. Разброс `perspective`: 0.53 п.п. (0.8859–0.8912) — числа растут с ростом `max_warp_frac`, что на первый взгляд похоже на тренд, но сам разброс в разы меньше собственного std каждой точки (±2.28–2.62 п.п.) — совсем не тот масштаб, что у `HOG_SIZE`'s реального эффекта в round 59 (там разброс многократно ПРЕВЫШАЛ std). 4 точки на шумовой кривой могут выглядеть монотонно случайно, особенно когда диапазон настолько тесен относительно шума — честный вывод: оба параметра остаются в пределах шума, полная 5-сидовая проверка не потребовалась. Текущие прод-значения (0.08 / 0.06) остаются разумным выбором. Все magnitude-параметры всех отгруженных аугментационных стадий (поворот, сдвиг, перспектива, zoom — последний отклонён) теперь хотя бы раз пересмотрены. Изменений в коде и чекпоинте нет.

**Round 64** (код-ревью фронтенда на безопасность; продолжение round 57/58/61, которые покрыли весь бэкенд, но не фронтенд): просмотрен `frontend/src/` на XSS, небезопасное перенаправление, хранение токена. **Без критичных замечаний**: нигде не используется `dangerouslySetInnerHTML`, `innerHTML` или `eval` — весь рендер идёт через обычный JSX (React экранирует по умолчанию), поэтому XSS-инъекция через данные с сервера или пользовательский ввод структурно исключена. Единственные редиректы (`window.location.href = "/login"` при 401) — жёстко заданная строка, не производная от параметров запроса — open redirect невозможен. Нет `target="_blank"` без `rel="noopener noreferrer"` (reverse tabnabbing не проверять, так как таких ссылок просто нет). JWT хранится в `localStorage` (`lib/api.ts`) — уязвим к эксфильтрации при XSS, но при отсутствии найденных XSS-точек внедрения это разумный, часто используемый выбор для SPA без серверных cookie-сессий. Единственное невысокоприоритетное наблюдение: ни `next.config.js`, ни `main.py`'s FastAPI-мидлвэр не выставляют заголовки безопасности (CSP/X-Frame-Options/HSTS) — обычно это делает reverse proxy перед приложением, не блокирующая находка без запроса именно на это. `tsc --noEmit` чист, изменений в коде нет.

**ML round 65** (oversampling миноритарного класса — перепроверено на текущем полном рецепте, снова честно отрицательно): round 32 проверил oversampling (фактор 2) на рецепте, каким он был тогда (поворот+отражение+яркость, до появления сдвига round 40 и перспективы round 44), и получил отрицательный результат. С тех пор рецепт вырос ещё сильнее — стоило перепроверить на актуальном полном рецепте (поворот+отражение+яркость+сдвиг+перспектива), а не полагаться на вывод на другом (пусть и похожем) пайплайне. База уже точно известна из `metrics.json`'s `multi_seed_evaluation` (те же 5 сидов) — пересчитана только сторона с oversampling, тем же протоколом честного held-out теста (не CV), что использует `_multi_seed_worker_evaluate`, с топ-апом миноритарного класса перед аугментацией (тот же порядок, что round 32):

| сид | accuracy: прод → +oversample | DME precision: прод → +oversample | DME recall: прод → +oversample |
|---|---|---|---|
| 42 | 0.9574 → 0.9489 ↓ | 0.8444 → 0.8372 ↓ | 0.9268 → 0.8780 ↓ |
| 7 | 0.9731 → 0.9686 ↓ | 0.9000 → 0.9259 ↑ | 0.9000 → 0.8333 ↓ |
| 99 | 0.9814 → 0.9767 ↓ | 0.9500 → 0.9474 ↓ | 0.8636 → 0.8182 ↓ |
| 123 | 0.9816 → 0.9724 ↓ | 0.9730 → 0.9714 ↓ | 0.9231 → 0.8718 ↓ |
| 2024 | 0.9690 → 0.9558 ↓ | 1.0000 → 0.9615 ↓ | 0.7941 → 0.7353 ↓ |
| **среднее** | **0.9725 → 0.9645** | **0.9335 → 0.9287** | **0.8815 → 0.8273 (±0.0513)** |

Честный вывод: accuracy просела **в 5 из 5 сидов**, DME precision — в 4 из 5 (выросла только на сиде 7), а DME recall — **тоже в 5 из 5 сидов**, в среднем на −5.4 п.п. — именно та метрика, ради которой oversampling обычно и пробуют, стала хуже на каждом без исключения сплите. Это не просадка в пределах шума: разброс направления (5/5, 5/5, 4/5) устойчиво отрицательный, тем же критерием «X из 5 сидов», которым в этом проекте оцениваются все аугментационные решения. Подтверждает и усиливает вывод round 32 на новом, гораздо большем рецепте — узкое место по-прежнему не в дисбалансе классов, а в потолке самих признаков; топ-ап миноритарного класса аугментированными копиями лишь размывает сигнал модели о том, что реально отличает DME. Флаг `--minority-oversample` остаётся в коде выключенным по умолчанию, без изменений. Изменений в чекпоинте нет.

Переобучить (или обучить на любом другом датасете с такой же структурой) можно так:

```bash
git clone https://github.com/Traslational-Visual-Health-Laboratory/OCT-AND-EYE-FUNDUS-DATASET.git
cd backend
python -m app.ml.train --data-dir /path/to/OCT-AND-EYE-FUNDUS-DATASET
```

Без `--data-dir` скрипт обучается на процедурно сгенерированной синтетике (`app/ml/synthetic_dataset.py`) — это smoke-test пайплайна, а не рабочая модель; полезно только чтобы быстро проверить, что код обучения/инференса не сломан.

Скрипт сохранит новый чекпоинт в `app/ml/artifacts/oct_classifier.joblib`; перезапустите backend, чтобы он подхватил новые веса (инференс кэширует загруженную модель в памяти процесса).

### Запуск через Docker Compose

```bash
cp backend/.env.example backend/.env
docker compose up --build
```

- Бэкенд: http://localhost:8000 (документация API — `/docs`)
- Фронтенд: http://localhost:3000

### Локальный запуск без Docker

Бэкенд (Python 3.12+):

```bash
cd backend
python3 -m venv .venv && source .venv/bin/activate
pip install -r requirements-dev.txt
uvicorn app.main:app --reload
```

По умолчанию используется SQLite (`backend/octera.db`), для Postgres задайте `DATABASE_URL` в `backend/.env`.

Тесты бэкенда:

```bash
cd backend
pytest
```

Фронтенд (Node.js 20+):

```bash
cd frontend
cp .env.local.example .env.local
npm install
npm run dev
```

Тесты фронтенда (Vitest + Testing Library):

```bash
cd frontend
npm test
```

### CI

`.github/workflows/ci.yml` на каждый push/PR гоняет `pytest` для бэкенда и `npm test` + `npm run build` для фронтенда.

## Миссия

Сделать высокоточную диагностику заболеваний сетчатки доступной каждой клинике независимо от ее размера, сократить время анализа исследований и повысить качество медицинской помощи за счет современных технологий искусственного интеллекта.

## Проблема

Сегодня большинство исследований ОКТ анализируются врачом вручную. Этот процесс имеет несколько недостатков:

- высокая нагрузка на специалистов;
- человеческий фактор;
- различия в опыте врачей;
- длительное время подготовки заключения;
- ограниченный доступ к опытным офтальмологам в небольших городах.

При увеличении количества исследований растет вероятность пропустить ранние признаки заболевания.

## Решение

OCTera автоматически анализирует медицинские изображения и помогает врачу принимать решения.

После загрузки ОКТ-снимка система:

1. Проверяет качество изображения.
2. Выполняет предварительную обработку данных.
3. Анализирует анатомические структуры сетчатки.
4. Выполняет сегментацию всех основных слоев.
5. Находит патологические изменения.
6. Определяет наиболее вероятные заболевания.
7. Рассчитывает вероятность каждого диагноза.
8. Строит карту обнаруженных патологий.
9. Генерирует подробное медицинское заключение.
10. Сохраняет результаты исследования.

Полный анализ занимает несколько секунд.

## Основные возможности

### Автоматический анализ ОКТ

Система определяет:

- возрастные изменения;
- макулярный отек;
- диабетический макулярный отек;
- возрастную макулярную дегенерацию;
- глаукому;
- эпиретинальную мембрану;
- серозную отслойку;
- нарушения структуры сетчатки;
- атрофические изменения;
- другие патологии.

### Сегментация

Нейросеть автоматически выделяет:

- внутренние и наружные слои сетчатки;
- область макулы;
- жидкость;
- кисты;
- зоны повреждений;
- патологические образования.

Полученная карта помогает врачу быстрее интерпретировать результаты исследования.

### AI-заключение

После анализа система автоматически формирует:

- описание исследования;
- найденные изменения;
- предполагаемый диагноз;
- уровень уверенности модели;
- рекомендации по дальнейшему обследованию.

Заключение может использоваться как предварительная версия для врача.

### Визуализация

Пользователь получает:

- исходное изображение;
- тепловую карту внимания модели;
- карту сегментации;
- выделенные патологические области;
- показатели толщины слоев;
- вероятности заболеваний.

### API

Платформа предоставляет REST API для интеграции с:

- медицинскими информационными системами;
- PACS;
- лабораторными системами;
- облачными сервисами;
- мобильными приложениями.

### История исследований

Для каждого исследования сохраняются изображения, результаты анализа и диагнозы — это уже реализовано (список своих исследований в личном кабинете). Сравнение динамики между исследованиями одного пользователя во времени — в планах (см. «Текущий этап продукта»).

## Архитектура

Платформа состоит из нескольких AI-модулей.

**Модуль проверки качества** — определяет достаточность качества изображения, наличие шумов, размытость, артефакты, ошибки сканирования.

**Модуль сегментации** — использует современные архитектуры компьютерного зрения для выделения анатомических структур глаза.

**Модуль диагностики** — на основе результатов сегментации определяет наличие заболеваний и рассчитывает вероятность каждого диагноза.

**Генератор медицинских заключений** — использует языковую модель для формирования структурированного текста заключения на русском языке.

**Модуль аналитики** — собирает статистику исследований, оценивает качество работы модели и отслеживает изменения ее эффективности.

## Безопасность

OCTera разрабатывается с учетом требований медицинских информационных систем. Предусмотрены:

- шифрование данных;
- разграничение прав доступа;
- журналирование действий пользователей;
- анонимизация персональных данных;
- защищенная передача информации;
- резервное копирование;
- аудит безопасности.

## Потенциальные пользователи

- государственные клиники;
- частные офтальмологические центры;
- диагностические лаборатории;
- медицинские университеты;
- исследовательские организации;
- телемедицинские платформы.

## Бизнес-модель

Планируется несколько вариантов использования продукта.

- **Self-service подписка** (ближайший этап) — физлицо регистрируется само и платит подписку за доступ к анализу собственных снимков, по аналогии с ChatGPT/Claude.
- **SaaS** — клиника оплачивает подписку и получает доступ к облачной платформе.
- **Enterprise** — развертывание системы на инфраструктуре медицинской организации.
- **API** — оплата за количество выполненных исследований через API.
- **Research** — лицензии для университетов и научных организаций.

## Долгосрочная цель

Создать универсальную AI-платформу для офтальмологии, которая станет стандартом автоматического анализа ОКТ-исследований, поможет врачам принимать более точные решения, сократит время диагностики и повысит доступность качественной медицинской помощи.
